/**
 * AUTO-GENERATED condition trace tests for all 1442 ability CSV rows.
 * Verifies that conditionForRow() correctly derives fires/blocks per
 * affects_self for every row without a complex conditional guard.
 *
 * Generated by: scripts/gen-condition-traces.py
 * DO NOT edit by hand — regenerate from CSV.
 */
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { conditionForRow } from '../../../src/engine/combat-conditions.js';

// Minimal game state — no map, no positions needed for self/others-none checks.
const G = {};

describe('0-0-0 — condition traces', () => {
  it('Invasive Procedure p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '0-0-0', ability: 'Invasive Procedure', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Unnerving p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: '0-0-0', ability: 'Unnerving', part: '1', affects_self: 'FALSE', affects_others: 'each adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: '0-0-0', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Shocking Palm p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: '0-0-0', ability: 'Shocking Palm', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: '0-0-0', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '0-0-0', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '0-0-0', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '0-0-0', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '0-0-0', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('4-LOM — condition traces', () => {
  it('Programming Override p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '4-LOM', ability: 'Programming Override', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '4-LOM', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Shared Intuition p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '4-LOM', ability: 'Shared Intuition', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Concussive Bolt p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '4-LOM', ability: 'Concussive Bolt', part: '1', affects_self: 'FALSE', affects_others: 'the target SMALL figure', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '4-LOM', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '4-LOM', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '4-LOM', ability: 'Surge: Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '4-LOM', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '4-LOM', ability: 'Surge: the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '4-LOM', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '4-LOM', ability: 'Surge: +1 Damage, +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '4-LOM', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('74-Z Speeder Bike (Elite) — condition traces', () => {
  it('Mounted p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '74-Z Speeder Bike (Elite)', ability: 'Mounted', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '74-Z Speeder Bike (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Thrusters p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '74-Z Speeder Bike (Elite)', ability: 'Thrusters', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Forward Mounted Blasters p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '74-Z Speeder Bike (Elite)', ability: 'Forward Mounted Blasters', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '74-Z Speeder Bike (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '74-Z Speeder Bike (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Evade token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '74-Z Speeder Bike (Elite)', ability: 'Surge: gain 1 Evade token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '74-Z Speeder Bike (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '74-Z Speeder Bike (Elite)', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '74-Z Speeder Bike (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('88-Z — condition traces', () => {
  it('88-Z p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '88-Z', ability: '88-Z', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '88-Z', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('A Powerful Influence — condition traces', () => {
  it('A Powerful Influence p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'A Powerful Influence', ability: 'A Powerful Influence', part: '1', affects_self: 'FALSE', affects_others: 'hostile figures within 3', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'A Powerful Influence', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('A Powerful Influence p2: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'A Powerful Influence', ability: 'A Powerful Influence', part: '2', affects_self: 'FALSE', affects_others: 'hostile figures within 3', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'A Powerful Influence', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('AT-DP — condition traces', () => {
  it('Assault p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-DP', ability: 'Assault', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-DP', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Charge Generators p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'AT-DP', ability: 'Charge Generators', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-DP', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'AT-DP', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'at_dp-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-DP', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-DP', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-DP', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-DP', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Massive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-DP', ability: 'Massive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-DP', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('AT-RT — condition traces', () => {
  it('Mortar Launcher p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'AT-RT', ability: 'Mortar Launcher', part: '1', affects_self: 'TRUE', affects_others: 'each figure on or adjacent to a chosen space within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Vanguard p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'AT-RT', ability: 'Vanguard', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-RT', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'AT-RT', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'at_rt-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-RT', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-RT', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-RT', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-RT', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-RT', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-RT', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('AT-ST — condition traces', () => {
  it('Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-ST', ability: 'Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-ST', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Awkward p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-ST', ability: 'Awkward', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-ST', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-ST', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-ST', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-ST', ability: 'Surge: Blast 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-ST', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-ST', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-ST', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Massive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'AT-ST', ability: 'Massive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'AT-ST', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Adrenaline — condition traces', () => {
  it('Adrenaline p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Adrenaline', ability: 'Adrenaline', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Adrenaline', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Adrenaline p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Adrenaline', ability: 'Adrenaline', part: '2', affects_self: 'TRUE', affects_others: 'your WOOKIEES', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Advance Warning — condition traces', () => {
  it('Advance Warning p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Advance Warning', ability: 'Advance Warning', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Advance Warning', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Against the Odds — condition traces', () => {
  it('Against the Odds p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Against the Odds', ability: 'Against the Odds', part: '1', affects_self: 'TRUE', affects_others: 'up to 3 of your figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Agent Blaise — condition traces', () => {
  it('Adapt p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Agent Blaise', ability: 'Adapt', part: '1', affects_self: 'TRUE', affects_others: '1 chosen friendly SPY or TROOPER', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Interrogate p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Blaise', ability: 'Interrogate', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Blaise', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Blaise', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Blaise', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Blaise', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Blaise', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Blaise', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Blaise', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Agent Kallus — condition traces', () => {
  it('Hunt Dissent p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Agent Kallus', ability: 'Hunt Dissent', part: '1', affects_self: 'TRUE', affects_others: 'friendly figures within 1', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Fulcrum p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Kallus', ability: 'Fulcrum', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Kallus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Bo-Rifle p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Kallus', ability: 'Bo-Rifle', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Kallus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened, you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Kallus', ability: 'Surge: the target becomes Weakened, you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Kallus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Kallus', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Kallus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Agent Kallus', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Agent Kallus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Ahsoka Tano — condition traces', () => {
  it('Force Leap p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Ahsoka Tano', ability: 'Force Leap', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Vigor p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ahsoka Tano', ability: 'Vigor', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ahsoka Tano', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Twin Sabers p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ahsoka Tano', ability: 'Twin Sabers', part: '1', affects_self: 'TRUE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ahsoka Tano', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ahsoka Tano', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ahsoka Tano', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ahsoka Tano', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ahsoka Tano', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Hit token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ahsoka Tano', ability: 'Surge: gain 1 Hit token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ahsoka Tano', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('All in a Day\'s Work — condition traces', () => {
  it('All in a Day\'s Work p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'All in a Day\'s Work', ability: 'All in a Day\'s Work', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Alliance Ranger (Elite) — condition traces', () => {
  it('Guerilla p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Alliance Ranger (Elite)', ability: 'Guerilla', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Elite Sniper p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Alliance Ranger (Elite)', ability: 'Elite Sniper', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Elite)', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Elite)', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Elite)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Alliance Ranger (Regular) — condition traces', () => {
  it('Guerilla p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Alliance Ranger (Regular)', ability: 'Guerilla', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Sniper p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Alliance Ranger (Regular)', ability: 'Sniper', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Regular)', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Regular)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Ranger (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Ranger (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Alliance Smuggler (Elite) — condition traces', () => {
  it('Improved Smuggler\'s Instincts p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Elite)', ability: 'Improved Smuggler\'s Instincts', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Slippery p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Elite)', ability: 'Slippery', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Alliance Smuggler (Elite)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'alliance_smuggler_elite-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Slippery p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Alliance Smuggler (Elite)', ability: 'Slippery', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Elite)', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Elite)', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Alliance Smuggler (Regular) — condition traces', () => {
  it('Smuggler\'s Instincts p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Regular)', ability: 'Smuggler\'s Instincts', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Slippery p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Regular)', ability: 'Slippery', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Alliance Smuggler (Regular)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'alliance_smuggler_regular-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Slippery p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Alliance Smuggler (Regular)', ability: 'Slippery', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Regular)', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Alliance Smuggler (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Alliance Smuggler (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Ambush — condition traces', () => {
  it('Ambush p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Ambush', ability: 'Ambush', part: '1', affects_self: 'TRUE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Apex Predator — condition traces', () => {
  it('Apex Predator p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Apex Predator', ability: 'Apex Predator', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Apex Predator', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Apex Predator p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Apex Predator', ability: 'Apex Predator', part: '2', affects_self: 'FALSE', affects_others: 'a hostile figure within 2', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Arcing Shot — condition traces', () => {
  it('Arcing Shot p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Arcing Shot', ability: 'Arcing Shot', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Armed Escort — condition traces', () => {
  it('Armed Escort p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Armed Escort', ability: 'Armed Escort', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Armed Escort', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Armed Escort p2: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Armed Escort', ability: 'Armed Escort', part: '2', affects_self: 'FALSE', affects_others: 'other friendly figures within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Armed Escort', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Assassinate — condition traces', () => {
  it('Assassinate p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Assassinate', ability: 'Assassinate', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('BD-1 — condition traces', () => {
  it('Damaged Scomplink p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'BD-1', ability: 'Damaged Scomplink', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'BD-1', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Stim Canister p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'BD-1', ability: 'Stim Canister', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Terminal Slicing p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'BD-1', ability: 'Terminal Slicing', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('BT-1 — condition traces', () => {
  it('Missile Salvo p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'BT-1', ability: 'Missile Salvo', part: '1', affects_self: 'FALSE', affects_others: 'the attack targets', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Assassin p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'BT-1', ability: 'Assassin', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'BT-1', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'BT-1', ability: 'Surge: Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'BT-1', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'BT-1', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'BT-1', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'BT-1', ability: 'Surge: Recover 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'BT-1', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Balancing Force — condition traces', () => {
  it('Balancing Force p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Balancing Force', ability: 'Balancing Force', part: '1', affects_self: 'TRUE', affects_others: 'up to 3 chosen figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Ballistics Matrix — condition traces', () => {
  it('Ballistics Matrix p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ballistics Matrix', ability: 'Ballistics Matrix', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ballistics Matrix', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Ballistics Matrix p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ballistics Matrix', ability: 'Ballistics Matrix', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ballistics Matrix', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Bantha Rider — condition traces', () => {
  it('Wild Beast p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bantha Rider', ability: 'Wild Beast', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Trample p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Bantha Rider', ability: 'Trample', part: '1', affects_self: 'FALSE', affects_others: 'up to 3 adjacent hostile figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Bantha Rider', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Stampede p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bantha Rider', ability: 'Stampede', part: '1', affects_self: 'FALSE', affects_others: 'each hostile figure in your space', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bantha Rider', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bantha Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bantha Rider', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bantha Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Massive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bantha Rider', ability: 'Massive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bantha Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Battle Scars — condition traces', () => {
  it('Battle Scars p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Battle Scars', ability: 'Battle Scars', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Battle Scars', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Battlefield Awareness — condition traces', () => {
  it('Battlefield Awareness p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Battlefield Awareness', ability: 'Battlefield Awareness', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Baze Malbus — condition traces', () => {
  it('Into the Fray p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Baze Malbus', ability: 'Into the Fray', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Assault p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Baze Malbus', ability: 'Assault', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Baze Malbus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Hold the Line p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Baze Malbus', ability: 'Hold the Line', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Baze Malbus', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Baze Malbus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, Cleave 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Baze Malbus', ability: 'Surge: +1 Damage, Cleave 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Baze Malbus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1, Cleave 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Baze Malbus', ability: 'Surge: Blast 1, Cleave 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Baze Malbus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Beatdown — condition traces', () => {
  it('Beatdown p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Beatdown', ability: 'Beatdown', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Beatdown', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Beatdown p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Beatdown', ability: 'Beatdown', part: '2', affects_self: 'TRUE', affects_others: 'friendly figures', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Behind Enemy Lines — condition traces', () => {
  it('Behind Enemy Lines p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Behind Enemy Lines', ability: 'Behind Enemy Lines', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Bib Fortuna — condition traces', () => {
  it('Bartered Information p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bib Fortuna', ability: 'Bartered Information', part: '1', affects_self: 'FALSE', affects_others: 'another friendly SCUM figure within 2', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Illicit Arms p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bib Fortuna', ability: 'Illicit Arms', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure that is attacking', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bib Fortuna', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bib Fortuna', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bib Fortuna', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bib Fortuna', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bib Fortuna', ability: 'Surge: +1 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bib Fortuna', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Biv Bodhrik — condition traces', () => {
  it('Multi-Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Biv Bodhrik', ability: 'Multi-Fire', part: '1', affects_self: 'FALSE', affects_others: 'the attack targets', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Suppression p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Biv Bodhrik', ability: 'Suppression', part: '1', affects_self: 'FALSE', affects_others: 'the target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Biv Bodhrik', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Biv Bodhrik', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'biv_bodhrik-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Biv Bodhrik', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Biv Bodhrik', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Biv Bodhrik', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Biv Bodhrik', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Biv Bodhrik', ability: 'Surge: Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Biv Bodhrik', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Black Market Prices — condition traces', () => {
  it('Black Market Prices p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Black Market Prices', ability: 'Black Market Prices', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Black Market Prices', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Bladestorm — condition traces', () => {
  it('Bladestorm p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bladestorm', ability: 'Bladestorm', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bladestorm', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Bladestorm p2: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Bladestorm', ability: 'Bladestorm', part: '2', affects_self: 'FALSE', affects_others: 'hostile figures within 2', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Bladestorm', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Blaze of Glory — condition traces', () => {
  it('Blaze of Glory p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Blaze of Glory', ability: 'Blaze of Glory', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Blaze of Glory p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Blaze of Glory', ability: 'Blaze of Glory', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Blaze of Glory', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Blend In — condition traces', () => {
  it('Blend In p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Blend In', ability: 'Blend In', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Blend In', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Blend In p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Blend In', ability: 'Blend In', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Blend In', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Blitz — condition traces', () => {
  it('Blitz p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Blitz', ability: 'Blitz', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Blitz', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Blood Feud — condition traces', () => {
  it('Blood Feud p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Blood Feud', ability: 'Blood Feud', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Blood Feud', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Blood Feud p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Blood Feud', ability: 'Blood Feud', part: '2', affects_self: 'TRUE', affects_others: 'figures in the marked group', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Bo-Katan Kryze — condition traces', () => {
  it('Beskar Armor p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bo-Katan Kryze', ability: 'Beskar Armor', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'self', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Dual-Wield Pistols p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bo-Katan Kryze', ability: 'Dual-Wield Pistols', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Last Wielder of the Darksaber p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bo-Katan Kryze', ability: 'Last Wielder of the Darksaber', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bo-Katan Kryze', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bo-Katan Kryze', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bo-Katan Kryze', ability: 'Surge: +3 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bo-Katan Kryze', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bo-Katan Kryze', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bo-Katan Kryze', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1, you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bo-Katan Kryze', ability: 'Surge: Pierce 1, you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bo-Katan Kryze', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bo-Katan Kryze', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bo-Katan Kryze', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Boba Fett — condition traces', () => {
  it('Wrist Cord p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Boba Fett', ability: 'Wrist Cord', part: '1', affects_self: 'TRUE', affects_others: 'a chosen SMALL figure within 3 in line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Wrist Flamethrower p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Boba Fett', ability: 'Wrist Flamethrower', part: '1', affects_self: 'FALSE', affects_others: 'each other figure on or adjacent to a chosen space within 2', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('EE-3 Carbine p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Boba Fett', ability: 'EE-3 Carbine', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Boba Fett', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Boba Fett', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Boba Fett', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'boba_fett-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Boba Fett', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Boba Fett', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'boba_fett-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Boba Fett', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Boba Fett', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Boba Fett', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Boba Fett', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Bodhi Rook — condition traces', () => {
  it('Smooth Landing p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bodhi Rook', ability: 'Smooth Landing', part: '1', affects_self: 'TRUE', affects_others: 'you and each adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bodhi Rook', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Air Support p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bodhi Rook', ability: 'Air Support', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure that is attacking', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bodhi Rook', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bodhi Rook', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bodhi Rook', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bodhi Rook', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bodhi Rook', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bodhi Rook', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Damage token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bodhi Rook', ability: 'Surge: gain 1 Damage token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bodhi Rook', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Bodyguard — condition traces', () => {
  it('Bodyguard p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bodyguard', ability: 'Bodyguard', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent friendly figure', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Bossk — condition traces', () => {
  it('Indiscriminate Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Bossk', ability: 'Indiscriminate Fire', part: '1', affects_self: 'FALSE', affects_others: 'each other figure within 2 of the target space other than the defender', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Regenerate p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bossk', ability: 'Regenerate', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bossk', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bossk', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bossk', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bossk', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Bossk', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'bossk-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('+2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bossk', ability: '+2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bossk', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Bossk', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Bossk', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Brace Yourself — condition traces', () => {
  it('Brace Yourself p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Brace Yourself', ability: 'Brace Yourself', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Brace for Impact — condition traces', () => {
  it('Brace for Impact p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Brace for Impact', ability: 'Brace for Impact', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Brace for Impact', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'brace_for_impact-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Built on Hope — condition traces', () => {
  it('Built on Hope p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Built on Hope', ability: 'Built on Hope', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Built on Hope', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Built on Hope p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Built on Hope', ability: 'Built on Hope', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Burst Fire — condition traces', () => {
  it('Burst Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Burst Fire', ability: 'Burst Fire', part: '1', affects_self: 'FALSE', affects_others: 'figures adjacent to the target space', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('C-3P0 — condition traces', () => {
  it('Inform p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'C-3P0', ability: 'Inform', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Cower p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'C-3P0', ability: 'Cower', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Distracting p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'C-3P0', ability: 'Distracting', part: '1', affects_self: 'FALSE', affects_others: 'a friendly figure that is defending', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Non-Combatant p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'C-3P0', ability: 'Non-Combatant', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'C-3P0', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('C1-10P "Chopper" — condition traces', () => {
  it('Ram p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'C1-10P "Chopper"', ability: 'Ram', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('System Shock p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'C1-10P "Chopper"', ability: 'System Shock', part: '1', affects_self: 'FALSE', affects_others: 'a figure on or adjacent to any terminal', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'C1-10P "Chopper"', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'C1-10P "Chopper"', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'C1-10P "Chopper"', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'C1-10P "Chopper"', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('CT-1701 — condition traces', () => {
  it('Barrage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'CT-1701', ability: 'Barrage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'CT-1701', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cover Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'CT-1701', ability: 'Cover Fire', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 3 spaces and the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'CT-1701', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'CT-1701', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Hit token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'CT-1701', ability: 'Surge: gain 1 Hit token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'CT-1701', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'CT-1701', ability: 'Surge: +2 Accuracy, the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'CT-1701', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Cad Bane — condition traces', () => {
  it('Flawless Execution p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cad Bane', ability: 'Flawless Execution', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('I Make the Rules Now p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cad Bane', ability: 'I Make the Rules Now', part: '1', affects_self: 'TRUE', affects_others: 'a friendly HUNTER within 4', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cad Bane', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cad Bane', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cad Bane', ability: 'Surge: Recover 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cad Bane', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cad Bane', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cad Bane', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Cal Kestis — condition traces', () => {
  it('Wall Run p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cal Kestis', ability: 'Wall Run', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cal Kestis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Force Slow p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cal Kestis', ability: 'Force Slow', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cal Kestis', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cal Kestis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cal Kestis', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cal Kestis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cal Kestis', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cal Kestis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Cal\'s Buddy — condition traces', () => {
  it('Cal\'s Buddy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cal\'s Buddy', ability: 'Cal\'s Buddy', part: '1', affects_self: 'TRUE', affects_others: 'the BD-1 Companion figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cal\'s Buddy', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Call the Vanguard — condition traces', () => {
  it('Call the Vanguard p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Call the Vanguard', ability: 'Call the Vanguard', part: '1', affects_self: 'TRUE', affects_others: 'a friendly TROOPER with figure cost 4 or greater', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Cam Droid — condition traces', () => {
  it('Agitate p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cam Droid', ability: 'Agitate', part: '1', affects_self: 'FALSE', affects_others: 'the defending figure', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cam Droid', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cam Droid', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cam Droid', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Cam Droid', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'cam_droid-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Camouflage — condition traces', () => {
  it('Camouflage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Camouflage', ability: 'Camouflage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Camouflage', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'camouflage-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Capitalize — condition traces', () => {
  it('Capitalize p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Capitalize', ability: 'Capitalize', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Capitalize', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Capitalize p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Capitalize', ability: 'Capitalize', part: '2', affects_self: 'TRUE', affects_others: 'the chosen die\'s roller', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Capitalize', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Capitalize p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Capitalize', ability: 'Capitalize', part: '3', affects_self: 'FALSE', affects_others: 'a hostile figure with a HARMFUL condition', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Captain Terro — condition traces', () => {
  it('Flamethrower p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Captain Terro', ability: 'Flamethrower', part: '1', affects_self: 'FALSE', affects_others: 'each other figure on or adjacent to a chosen space within 2', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Mounted p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Captain Terro', ability: 'Mounted', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Captain Terro', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Captain Terro', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Captain Terro', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Captain Terro', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Captain Terro', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Captain Terro', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Captain Terro', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Captain Terro', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Captain Terro', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Capture the Weary — condition traces', () => {
  it('Capture the Weary p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Capture the Weary', ability: 'Capture the Weary', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Cara Dune — condition traces', () => {
  it('Shock and Awe p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cara Dune', ability: 'Shock and Awe', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cara Dune', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Smash p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cara Dune', ability: 'Smash', part: '1', affects_self: 'TRUE', affects_others: '1 adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cara Dune', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Hunker Down p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cara Dune', ability: 'Hunker Down', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cara Dune', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cara Dune', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cara Dune', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cara Dune', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cara Dune', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cara Dune', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cara Dune', ability: 'Surge: Blast 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cara Dune', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Cassian Andor — condition traces', () => {
  it('Strike Team p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cassian Andor', ability: 'Strike Team', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent friendly figure and up to 4 friendly figures outside your deployment zone', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cassian Andor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('It Will be Alright p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cassian Andor', ability: 'It Will be Alright', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 2 that can be defeated', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cassian Andor', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cassian Andor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cassian Andor', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cassian Andor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 2 Damage token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cassian Andor', ability: 'Surge: gain 2 Damage token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cassian Andor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Cavalry Charge — condition traces', () => {
  it('Cavalry Charge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cavalry Charge', ability: 'Cavalry Charge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cavalry Charge', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cavalry Charge p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cavalry Charge', ability: 'Cavalry Charge', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cavalry Charge', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cavalry Charge p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cavalry Charge', ability: 'Cavalry Charge', part: '3', affects_self: 'TRUE', affects_others: 'friendly TROOPER within 3', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Celebration — condition traces', () => {
  it('Celebration p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Celebration', ability: 'Celebration', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Change of Plans — condition traces', () => {
  it('Change of Plans p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Change of Plans', ability: 'Change of Plans', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Chaotic Force — condition traces', () => {
  it('Chaotic Force p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Chaotic Force', ability: 'Chaotic Force', part: '1', affects_self: 'TRUE', affects_others: 'up to 3 chosen figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Cheat to Win — condition traces', () => {
  it('Cheat to Win p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cheat to Win', ability: 'Cheat to Win', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Chewbacca — condition traces', () => {
  it('Slam p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chewbacca', ability: 'Slam', part: '1', affects_self: 'TRUE', affects_others: '1 adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chewbacca', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Protector p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Chewbacca', ability: 'Protector', part: '1', affects_self: 'FALSE', affects_others: 'a friendly figure being targeted', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chewbacca', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chewbacca', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chewbacca', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chewbacca', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chewbacca', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chewbacca', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Chirrut Imwe — condition traces', () => {
  it('Devout p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chirrut Imwe', ability: 'Devout', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chirrut Imwe', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('I\'m One With the Force p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Chirrut Imwe', ability: 'I\'m One With the Force', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('The Force is With Me p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Chirrut Imwe', ability: 'The Force is With Me', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chirrut Imwe', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chirrut Imwe', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chirrut Imwe', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chirrut Imwe', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Chirrut Imwe', ability: 'Surge: Cleave 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Chirrut Imwe', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Choose a Side — condition traces', () => {
  it('Choose a Side p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Choose a Side', ability: 'Choose a Side', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Choose a Side', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Choose a Side p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Choose a Side', ability: 'Choose a Side', part: '2', affects_self: 'FALSE', affects_others: 'other friendly Mobile figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Clawdite Shapeshifter (Elite) — condition traces', () => {
  it('Shape p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Clawdite Shapeshifter (Elite)', ability: 'Shape', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Clawdite Shapeshifter (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Shift p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Clawdite Shapeshifter (Elite)', ability: 'Shift', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Clawdite Shapeshifter (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Clawdite Shapeshifter (Regular) — condition traces', () => {
  it('Shape p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Clawdite Shapeshifter (Regular)', ability: 'Shape', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Clawdite Shapeshifter (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Shift p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Clawdite Shapeshifter (Regular)', ability: 'Shift', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Clawdite Shapeshifter (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Cloned Reinforcements — condition traces', () => {
  it('Cloned Reinforcements p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Cloned Reinforcements', ability: 'Cloned Reinforcements', part: '1', affects_self: 'TRUE', affects_others: 'a defeated friendly non-DROID non-unique TROOPER', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Close and Personal — condition traces', () => {
  it('Close and Personal p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Close and Personal', ability: 'Close and Personal', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Close and Personal', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Close the Gap — condition traces', () => {
  it('Close the Gap p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Close the Gap', ability: 'Close the Gap', part: '1', affects_self: 'TRUE', affects_others: 'friendly BRAWLERS', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Close the Gap', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Collateral Damage — condition traces', () => {
  it('Collateral Damage p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Collateral Damage', ability: 'Collateral Damage', part: '1', affects_self: 'FALSE', affects_others: 'a figure or object other than the defender within 2 of the target space', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Collect Intel — condition traces', () => {
  it('Collect Intel p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Collect Intel', ability: 'Collect Intel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Combat Resupply — condition traces', () => {
  it('Combat Resupply p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Combat Resupply', ability: 'Combat Resupply', part: '1', affects_self: 'TRUE', affects_others: 'friendly figures within 3', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Combat Resupply', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Comm Disruption — condition traces', () => {
  it('Comm Disruption p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Comm Disruption', ability: 'Comm Disruption', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Concentrated Fire — condition traces', () => {
  it('Concentrated Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Concentrated Fire', ability: 'Concentrated Fire', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Coordinated Attack — condition traces', () => {
  it('Coordinated Attack p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Coordinated Attack', ability: 'Coordinated Attack', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Corrupting Force — condition traces', () => {
  it('Corrupting Force p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Corrupting Force', ability: 'Corrupting Force', part: '1', affects_self: 'TRUE', affects_others: 'up to 3 chosen figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Counter Attack — condition traces', () => {
  it('Counter Attack p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Counter Attack', ability: 'Counter Attack', part: '1', affects_self: 'TRUE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Covering Fire — condition traces', () => {
  it('Covering Fire p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Covering Fire', ability: 'Covering Fire', part: '1', affects_self: 'TRUE', affects_others: 'up to 3 friendly TROOPERS', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Covering Fire', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Covering Fire p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Covering Fire', ability: 'Covering Fire', part: '2', affects_self: 'TRUE', affects_others: 'each of your TROOPERS', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Covering Fire', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Cripple — condition traces', () => {
  it('Cripple p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Cripple', ability: 'Cripple', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Cripple', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Cruel Strike — condition traces', () => {
  it('Cruel Strike p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cruel Strike', ability: 'Cruel Strike', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cruel Strike', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Crush — condition traces', () => {
  it('Crush p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Crush', ability: 'Crush', part: '1', affects_self: 'TRUE', affects_others: 'a SMALL figure in your ending space', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Cut Lines — condition traces', () => {
  it('Cut Lines p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Cut Lines', ability: 'Cut Lines', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Cut Lines', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Dangerous Bargains — condition traces', () => {
  it('Dangerous Bargains p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Dangerous Bargains', ability: 'Dangerous Bargains', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Dangerous Prey — condition traces', () => {
  it('Dangerous Prey p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Dangerous Prey', ability: 'Dangerous Prey', part: '1', affects_self: 'TRUE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Dark Energy — condition traces', () => {
  it('Dark Energy p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Dark Energy', ability: 'Dark Energy', part: '1', affects_self: 'FALSE', affects_others: 'another SMALL figure within 3', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Dark Energy', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Dark Trooper Mk III — condition traces', () => {
  it('Advanced Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dark Trooper Mk III', ability: 'Advanced Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dark Trooper Mk III', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Durasteel Fist p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dark Trooper Mk III', ability: 'Durasteel Fist', part: '1', affects_self: 'TRUE', affects_others: '1 adjacent figure or object', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dark Trooper Mk III', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Lift Off p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dark Trooper Mk III', ability: 'Lift Off', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dark Trooper Mk III', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dark Trooper Mk III', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dark Trooper Mk III', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dark Trooper Mk III', ability: 'Surge: +2 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dark Trooper Mk III', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Darth Vader — condition traces', () => {
  it('Brutality p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Darth Vader', ability: 'Brutality', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Darth Vader', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Force Choke p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Darth Vader', ability: 'Force Choke', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure in your line of sight', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Darth Vader', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Foresight p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Darth Vader', ability: 'Foresight', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Darth Vader', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'darth_vader-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Darth Vader', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Darth Vader', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Darth Vader', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Darth Vader', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Data Theft — condition traces', () => {
  it('Data Theft p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Data Theft', ability: 'Data Theft', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Data Theft', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Davith Elso — condition traces', () => {
  it('Stealthy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Davith Elso', ability: 'Stealthy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Davith Elso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cut and Run p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Davith Elso', ability: 'Cut and Run', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure whose space you exit', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Fell Swoop p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Davith Elso', ability: 'Fell Swoop', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Davith Elso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage, you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Davith Elso', ability: 'Surge: +2 Damage, you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Davith Elso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Davith Elso', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Davith Elso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('De Wanna Wanga — condition traces', () => {
  it('De Wanna Wanga p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'De Wanna Wanga', ability: 'De Wanna Wanga', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'De Wanna Wanga', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('De Wanna Wanga p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'De Wanna Wanga', ability: 'De Wanna Wanga', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Deadeye — condition traces', () => {
  it('Deadeye p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Deadeye', ability: 'Deadeye', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Deadeye', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Deadeye p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Deadeye', ability: 'Deadeye', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Deadeye', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Deadly Precision — condition traces', () => {
  it('Deadly Precision p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Deadly Precision', ability: 'Deadly Precision', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Deadly Precision', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Deadly Precision p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Deadly Precision', ability: 'Deadly Precision', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Death Trooper (Elite) — condition traces', () => {
  it('Squad Captain p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Elite)', ability: 'Squad Captain', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly TROOPER or LEADER', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Death Trooper (Elite)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Field Tactics p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Death Trooper (Elite)', ability: 'Field Tactics', part: '1', affects_self: 'TRUE', affects_others: 'a friendly TROOPER or LEADER group with cost 6 or less', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+4 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Elite)', ability: '+4 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Death Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Death Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Elite)', ability: 'Surge: +2 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Death Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Death Trooper (Regular) — condition traces', () => {
  it('Security Detail p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Regular)', ability: 'Security Detail', part: '1', affects_self: 'TRUE', affects_others: 'a friendly LEADER', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Death Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Field Tactics p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Death Trooper (Regular)', ability: 'Field Tactics', part: '1', affects_self: 'TRUE', affects_others: 'a friendly TROOPER or LEADER group with cost 6 or less', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Regular)', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Death Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Death Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Death Trooper (Regular)', ability: 'Surge: +2 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Death Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Deathblow — condition traces', () => {
  it('Deathblow p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Deathblow', ability: 'Deathblow', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Deathblow p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Deathblow', ability: 'Deathblow', part: '2', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Debts Repaid — condition traces', () => {
  it('Debts Repaid p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Debts Repaid', ability: 'Debts Repaid', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Definition: \'Love\' — condition traces', () => {
  it('Definition: \'Love\' p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Definition: \'Love\'', ability: 'Definition: \'Love\'', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Deflection — condition traces', () => {
  it('Deflection p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Deflection', ability: 'Deflection', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Deflection p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Deflection', ability: 'Deflection', part: '2', affects_self: 'FALSE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Del Meeko — condition traces', () => {
  it('Expertise p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Del Meeko', ability: 'Expertise', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Gifted Mechanic p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Del Meeko', ability: 'Gifted Mechanic', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent friendly DROID or VEHICLE', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Del Meeko', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Open-Minded p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Del Meeko', ability: 'Open-Minded', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Del Meeko', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Del Meeko', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Del Meeko', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Del Meeko', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Del Meeko', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Demoralizing Monologue — condition traces', () => {
  it('Demoralizing Monologue p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Demoralizing Monologue', ability: 'Demoralizing Monologue', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Demoralizing Monologue', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Demoralizing Monologue p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Demoralizing Monologue', ability: 'Demoralizing Monologue', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Dengar — condition traces', () => {
  it('Spread the Pain p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Dengar', ability: 'Spread the Pain', part: '1', affects_self: 'FALSE', affects_others: 'a figure on or adjacent to the target space', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dengar', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Dengar', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'dengar-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dengar', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dengar', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dengar', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dengar', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Deploy the Garrison — condition traces', () => {
  it('Deploy the Garrison p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Deploy the Garrison', ability: 'Deploy the Garrison', part: '1', affects_self: 'TRUE', affects_others: 'each friendly TROOPER or GUARDIAN within 4', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Deploy the Garrison', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Desperate Escape — condition traces', () => {
  it('Desperate Escape p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Desperate Escape', ability: 'Desperate Escape', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Desperate Escape', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Desperate Escape p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Desperate Escape', ability: 'Desperate Escape', part: '2', affects_self: 'TRUE', affects_others: 'friendly figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Devotion — condition traces', () => {
  it('Devotion p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Devotion', ability: 'Devotion', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Dewback Rider — condition traces', () => {
  it('Shock Lance p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dewback Rider', ability: 'Shock Lance', part: '1', affects_self: 'TRUE', affects_others: 'a figure within 2 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dewback Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mounted p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dewback Rider', ability: 'Mounted', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dewback Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dewback Rider', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dewback Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dewback Rider', ability: 'Surge: +1 Damage, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dewback Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dewback Rider', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dewback Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dewback Rider', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dewback Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dewback Rider', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dewback Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Diala Passil — condition traces', () => {
  it('Battle Meditation p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Diala Passil', ability: 'Battle Meditation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Diala Passil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Defensive Stance p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Diala Passil', ability: 'Defensive Stance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Diala Passil', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'diala_passil-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Force Throw p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Diala Passil', ability: 'Force Throw', part: '1', affects_self: 'FALSE', affects_others: 'another small figure within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Diala Passil', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Diala Passil', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Diala Passil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Diala Passil', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Diala Passil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Diala Passil', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Diala Passil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Dio — condition traces', () => {
  it('Attached p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Dio', ability: 'Attached', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Insignificant p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Dio', ability: 'Insignificant', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage, the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dio', ability: 'Surge: +1 Damage, the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dio', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dio', ability: 'Surge: +1 Damage, +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dio', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dio', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dio', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Dioxis Fumes — condition traces', () => {
  it('Dioxis Fumes p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dioxis Fumes', ability: 'Dioxis Fumes', part: '1', affects_self: 'TRUE', affects_others: 'each non-DROID figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dioxis Fumes', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Dioxis Fumes p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dioxis Fumes', ability: 'Dioxis Fumes', part: '2', affects_self: 'TRUE', affects_others: 'non-Droid figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dioxis Fumes', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Director Krennic — condition traces', () => {
  it('Advanced Weapons Research p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Director Krennic', ability: 'Advanced Weapons Research', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 2 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Director Krennic', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Unhinged Director p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Director Krennic', ability: 'Unhinged Director', part: '1', affects_self: 'TRUE', affects_others: 'a friendly TROOPER or GUARDIAN within 2 spaces', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Surge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Director Krennic', ability: '+1 Surge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Director Krennic', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage, +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Director Krennic', ability: 'Surge: +2 Damage, +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Director Krennic', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Director Krennic', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Director Krennic', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Dirty Trick — condition traces', () => {
  it('Dirty Trick p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Dirty Trick', ability: 'Dirty Trick', part: '1', affects_self: 'TRUE', affects_others: 'the entering hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Disable — condition traces', () => {
  it('Disable p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Disable', ability: 'Disable', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Disarm — condition traces', () => {
  it('Disarm p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Disarm', ability: 'Disarm', part: '1', affects_self: 'FALSE', affects_others: 'the adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Disengage — condition traces', () => {
  it('Disengage p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Disengage', ability: 'Disengage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Disorient — condition traces', () => {
  it('Disorient p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Disorient', ability: 'Disorient', part: '1', affects_self: 'TRUE', affects_others: 'the figure that suffered Damage', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Doctor Aphra — condition traces', () => {
  it('Dubious Counterparts p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Doctor Aphra', ability: 'Dubious Counterparts', part: '1', affects_self: 'TRUE', affects_others: 'a friendly DROID', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Excavation p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Doctor Aphra', ability: 'Excavation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Doctor Aphra', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Doctor Aphra', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Doctor Aphra', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Doctor Aphra', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Double or Nothing — condition traces', () => {
  it('Double or Nothing p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Double or Nothing', ability: 'Double or Nothing', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Dr. Royce Hemlock — condition traces', () => {
  it('Neurotoxin p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Dr. Royce Hemlock', ability: 'Neurotoxin', part: '1', affects_self: 'FALSE', affects_others: 'all hostile figures on or adjacent to the chosen space within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Dr. Royce Hemlock', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Neurostim p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Dr. Royce Hemlock', ability: 'Neurostim', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Dr. Royce Hemlock', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dr. Royce Hemlock', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dr. Royce Hemlock', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dr. Royce Hemlock', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dr. Royce Hemlock', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Dr. Royce Hemlock', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Dr. Royce Hemlock', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Draw! — condition traces', () => {
  it('Draw! p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Draw!', ability: 'Draw!', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Draw!', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Droid Mastery — condition traces', () => {
  it('Droid Mastery p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Droid Mastery', ability: 'Droid Mastery', part: '1', affects_self: 'TRUE', affects_others: 'J4X-7', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Droid Mastery', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Drokkatta — condition traces', () => {
  it('Demolish p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Drokkatta', ability: 'Demolish', part: '1', affects_self: 'TRUE', affects_others: 'each figure on or adjacent to the chosen space within 3 spaces and line of sight', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Drokkatta', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Shrapnel p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Drokkatta', ability: 'Shrapnel', part: '1', affects_self: 'FALSE', affects_others: 'each figure and object within 2 spaces of the target space', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Drokkatta', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Drokkatta', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Drokkatta', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Drokkatta', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Drokkatta', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Drokkatta', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'drokkatta-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Drokkatta', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Drokkatta', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Dying Lunge — condition traces', () => {
  it('Dying Lunge p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Dying Lunge', ability: 'Dying Lunge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('E-Web Engineer (Elite) — condition traces', () => {
  it('Forward Emplacement p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: 'Forward Emplacement', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Tripod p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: 'Tripod', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Assault p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: 'Assault', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'e_web_engineer_elite-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Elite)', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('E-Web Engineer (Regular) — condition traces', () => {
  it('Tripod p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Regular)', ability: 'Tripod', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Assault p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Regular)', ability: 'Assault', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Regular)', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Regular)', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'E-Web Engineer (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'E-Web Engineer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Echo Base Trooper (Elite) — condition traces', () => {
  it('Front Line p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Echo Base Trooper (Elite)', ability: 'Front Line', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Cortosis Weave p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Echo Base Trooper (Elite)', ability: 'Cortosis Weave', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Echo Base Trooper (Elite)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'echo_base_trooper_elite-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Echo Base Trooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Echo Base Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Echo Base Trooper (Elite)', ability: 'Surge: +2 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Echo Base Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Echo Base Trooper (Elite)', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Echo Base Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Echo Base Trooper (Regular) — condition traces', () => {
  it('Front Line p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Echo Base Trooper (Regular)', ability: 'Front Line', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Echo Base Trooper (Regular)', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Echo Base Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Echo Base Trooper (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Echo Base Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Echo Base Trooper (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Echo Base Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Eerie Visage — condition traces', () => {
  it('Eerie Visage p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Eerie Visage', ability: 'Eerie Visage', part: '1', affects_self: 'FALSE', affects_others: 'each hostile figure with line of sight to you', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Efficient Travel — condition traces', () => {
  it('Efficient Travel p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Efficient Travel', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'each of your figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Element of Surprise — condition traces', () => {
  it('Element of Surprise p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Element of Surprise', ability: 'Element of Surprise', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Elusive — condition traces', () => {
  it('Elusive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Elusive', ability: 'Elusive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Elusive', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'elusive-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Emergency Aid — condition traces', () => {
  it('Emergency Aid p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Emergency Aid', ability: 'Emergency Aid', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Emergency Aid p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Emergency Aid', ability: 'Emergency Aid', part: '2', affects_self: 'TRUE', affects_others: 'an adjacent figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Emperor Palpatine — condition traces', () => {
  it('Emperor p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Emperor Palpatine', ability: 'Emperor', part: '1', affects_self: 'FALSE', affects_others: 'another figure within 4 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Emperor Palpatine', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Tempt p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Emperor Palpatine', ability: 'Tempt', part: '1', affects_self: 'TRUE', affects_others: 'a figure of your choice', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Emperor Palpatine', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Force Lightning p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Emperor Palpatine', ability: 'Force Lightning', part: '1', affects_self: 'FALSE', affects_others: 'a figure within 4 spaces and line of sight and each figure adjacent to it', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Emperor Palpatine', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Emperor Palpatine', ability: 'Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Emperor Palpatine', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Emperor Palpatine', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Emperor Palpatine', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Endless Reserves — condition traces', () => {
  it('Endless Reserves p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Endless Reserves', ability: 'Endless Reserves', part: '1', affects_self: 'TRUE', affects_others: 'a defeated friendly TROOPER', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Escalating Hostility — condition traces', () => {
  it('Escalating Hostility p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Escalating Hostility', ability: 'Escalating Hostility', part: '1', affects_self: 'FALSE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Espionage Mastery — condition traces', () => {
  it('Espionage Mastery p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Espionage Mastery', ability: 'Espionage Mastery', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Etiquette and Protocol — condition traces', () => {
  it('Etiquette and Protocol p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Etiquette and Protocol', ability: 'Etiquette and Protocol', part: '1', affects_self: 'FALSE', affects_others: '1 hostile and 1 friendly figure in your line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Evacuate — condition traces', () => {
  it('Evacuate p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Evacuate', ability: 'Evacuate', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 2 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Ewok Warrior (Elite) — condition traces', () => {
  it('Ambush p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ewok Warrior (Elite)', ability: 'Ambush', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ewok Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Forest Fighters p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Ewok Warrior (Elite)', ability: 'Forest Fighters', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Sling Barrage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ewok Warrior (Elite)', ability: 'Sling Barrage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ewok Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ewok Warrior (Elite)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ewok Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ewok Warrior (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ewok Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1, you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ewok Warrior (Elite)', ability: 'Surge: Pierce 1, you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ewok Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Explosive Weaponry — condition traces', () => {
  it('Explosive Weaponry p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Explosive Weaponry', ability: 'Explosive Weaponry', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Expose Weakness — condition traces', () => {
  it('Expose Weakness p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Expose Weakness', ability: 'Expose Weakness', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Expose Weakness p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Expose Weakness', ability: 'Expose Weakness', part: '2', affects_self: 'TRUE', affects_others: 'the chosen figure', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Extra Protection — condition traces', () => {
  it('Extra Protection p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Extra Protection', ability: 'Extra Protection', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 2', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Extra Protection p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Extra Protection', ability: 'Extra Protection', part: '2', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Extra Protection', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Eyes on the Prize — condition traces', () => {
  it('Eyes on the Prize p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Eyes on the Prize', ability: 'Eyes on the Prize', part: '1', affects_self: 'TRUE', affects_others: 'each friendly figure carrying or controlling a crate or mission token', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Ezra Bridger — condition traces', () => {
  it('Brash p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ezra Bridger', ability: 'Brash', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ezra Bridger', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Much to Learn p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Ezra Bridger', ability: 'Much to Learn', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ezra Bridger', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Ezra Bridger', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'ezra_bridger-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ezra Bridger', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ezra Bridger', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ezra Bridger', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ezra Bridger', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ezra Bridger', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ezra Bridger', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Face Me! — condition traces', () => {
  it('Face Me! p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Face Me!', ability: 'Face Me!', part: '1', affects_self: 'FALSE', affects_others: 'a unique hostile figure with line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Face to Face — condition traces', () => {
  it('Face to Face p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Face to Face', ability: 'Face to Face', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent figure or object', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Face to Face', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Fatal Deception — condition traces', () => {
  it('Fatal Deception p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Fatal Deception', ability: 'Fatal Deception', part: '1', affects_self: 'TRUE', affects_others: 'a figure with figure cost 5 or less within 5', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Feint — condition traces', () => {
  it('Feint p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Feint', ability: 'Feint', part: '1', affects_self: 'TRUE', affects_others: 'a figure within 2 spaces', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Fenn Signis — condition traces', () => {
  it('Havoc Shot p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Fenn Signis', ability: 'Havoc Shot', part: '1', affects_self: 'TRUE', affects_others: 'up to 2 figures within 2 spaces of the target space in your line of sight', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Tactical Movement p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fenn Signis', ability: 'Tactical Movement', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fenn Signis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fenn Signis', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fenn Signis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fenn Signis', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fenn Signis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1, Recover 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fenn Signis', ability: 'Surge: Blast 1, Recover 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fenn Signis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fenn Signis', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fenn Signis', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Fennec Shand — condition traces', () => {
  it('Bounty p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fennec Shand', ability: 'Bounty', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fennec Shand', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Sharpshooter p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Fennec Shand', ability: 'Sharpshooter', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fennec Shand', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Fennec Shand', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'fennec_shand-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fennec Shand', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fennec Shand', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fennec Shand', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fennec Shand', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Feral Swipes — condition traces', () => {
  it('Feral Swipes p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Feral Swipes', ability: 'Feral Swipes', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Feral Swipes', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Ferocity — condition traces', () => {
  it('Ferocity p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ferocity', ability: 'Ferocity', part: '1', affects_self: 'TRUE', affects_others: 'one of your or your opponent\'s CREATURES', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ferocity', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Field Promotion — condition traces', () => {
  it('Field Promotion p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Field Promotion', ability: 'Field Promotion', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Field Supply — condition traces', () => {
  it('Field Supply p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Field Supply', ability: 'Field Supply', part: '1', affects_self: 'FALSE', affects_others: 'up to 2 other figures within 3', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Field Supply', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Field Supply p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Field Supply', ability: 'Field Supply', part: '2', affects_self: 'TRUE', affects_others: 'a friendly figure who spends a Hit Token or Surge Token', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Field Tactician — condition traces', () => {
  it('Field Tactician p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Field Tactician', ability: 'Field Tactician', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Field Tactician', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Fifth Brother — condition traces', () => {
  it('Vigor p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fifth Brother', ability: 'Vigor', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fifth Brother', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Relentless Pursuit p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Fifth Brother', ability: 'Relentless Pursuit', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Fifth Brother', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Sith Acolyte p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fifth Brother', ability: 'Sith Acolyte', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fifth Brother', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cleave 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fifth Brother', ability: 'Cleave 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fifth Brother', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fifth Brother', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fifth Brother', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fifth Brother', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fifth Brother', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Final Stand — condition traces', () => {
  it('Final Stand p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Final Stand', ability: 'Final Stand', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Findsman Meditation — condition traces', () => {
  it('Findsman Meditation p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Findsman Meditation', ability: 'Findsman Meditation', part: '1', affects_self: 'TRUE', affects_others: 'one of your opponent\'s groups', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Findsman Meditation', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Findsman Meditation p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Findsman Meditation', ability: 'Findsman Meditation', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Fleet Footed — condition traces', () => {
  it('Fleet Footed p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fleet Footed', ability: 'Fleet Footed', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fleet Footed', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Flurry of Blades — condition traces', () => {
  it('Flurry of Blades p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Flurry of Blades', ability: 'Flurry of Blades', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Flurry of Blades', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Focus — condition traces', () => {
  it('Focus p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Focus', ability: 'Focus', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Focus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Fool Me Once — condition traces', () => {
  it('Fool Me Once p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fool Me Once', ability: 'Fool Me Once', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fool Me Once', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Forbidden Knowledge — condition traces', () => {
  it('Forbidden Knowledge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Forbidden Knowledge', ability: 'Forbidden Knowledge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Forbidden Knowledge', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Force Drain — condition traces', () => {
  it('Force Drain p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Force Drain', ability: 'Force Drain', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Force Drain', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Force Drain p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Force Drain', ability: 'Force Drain', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Force Illusion — condition traces', () => {
  it('Force Illusion p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Force Illusion', ability: 'Force Illusion', part: '1', affects_self: 'TRUE', affects_others: 'the defender', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Force Jump — condition traces', () => {
  it('Force Jump p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Force Jump', ability: 'Force Jump', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Force Jump', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Force Lightning — condition traces', () => {
  it('Force Lightning p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Force Lightning', ability: 'Force Lightning', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 2 and each figure adjacent to it', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Force Push — condition traces', () => {
  it('Force Push p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Force Push', ability: 'Force Push', part: '1', affects_self: 'FALSE', affects_others: 'another SMALL figure within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Force Rush — condition traces', () => {
  it('Force Rush p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Force Rush', ability: 'Force Rush', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Force Rush', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Force Surge — condition traces', () => {
  it('Force Surge p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Force Surge', ability: 'Force Surge', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Force Surge', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Foresee — condition traces', () => {
  it('Foresee p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Foresee', ability: 'Foresee', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Forward March — condition traces', () => {
  it('Forward March p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Forward March', ability: 'Forward March', part: '1', affects_self: 'TRUE', affects_others: 'each friendly figure within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Forward March', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Fuel Upgrade — condition traces', () => {
  it('Fuel Upgrade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Fuel Upgrade', ability: 'Fuel Upgrade', part: '1', affects_self: 'TRUE', affects_others: 'each of your VEHICLES', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Fuel Upgrade', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Furious Charge — condition traces', () => {
  it('Furious Charge p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Furious Charge', ability: 'Furious Charge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Gaarkhan — condition traces', () => {
  it('Brutal Cleave p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gaarkhan', ability: 'Brutal Cleave', part: '1', affects_self: 'TRUE', affects_others: 'a different figure or object', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gaarkhan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Charge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gaarkhan', ability: 'Charge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gaarkhan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gaarkhan', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gaarkhan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Bleed p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gaarkhan', ability: 'Bleed', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gaarkhan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gaarkhan', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Gaarkhan', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'gaarkhan-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gaarkhan', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gaarkhan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gaarkhan', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gaarkhan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Gamorrean Guard (Elite) — condition traces', () => {
  it('Gamorrean Honor Guard p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Gamorrean Guard (Elite)', ability: 'Gamorrean Honor Guard', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gamorrean Guard (Elite)', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gamorrean Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gamorrean Guard (Elite)', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gamorrean Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gamorrean Guard (Elite)', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gamorrean Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Gar Saxon — condition traces', () => {
  it('Airborne Commander p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Gar Saxon', ability: 'Airborne Commander', part: '1', affects_self: 'FALSE', affects_others: 'friendly Mobile figures within 4', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Gar Saxon', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Personal Combat Shield p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Gar Saxon', ability: 'Personal Combat Shield', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Gar Saxon\'s Flamethrower p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Gar Saxon', ability: 'Gar Saxon\'s Flamethrower', part: '1', affects_self: 'FALSE', affects_others: 'each other figure on or adjacent to a chosen space within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Gar Saxon', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gar Saxon', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gar Saxon', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gar Saxon', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gar Saxon', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Block token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gar Saxon', ability: 'Surge: gain 1 Block token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gar Saxon', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gar Saxon', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gar Saxon', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gar Saxon', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gar Saxon', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Gauntlet Blade — condition traces', () => {
  it('Gauntlet Blade p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Gauntlet Blade', ability: 'Gauntlet Blade', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('General Sorin — condition traces', () => {
  it('Bombardment p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'General Sorin', ability: 'Bombardment', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'General Sorin', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Advanced Firepower p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'General Sorin', ability: 'Advanced Firepower', part: '1', affects_self: 'FALSE', affects_others: 'adjacent friendly DROIDS and VEHICLES', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'General Sorin', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Sorin', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Sorin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Sorin', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Sorin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Sorin', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Sorin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('General Weiss — condition traces', () => {
  it('General\'s Orders p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'General Weiss', ability: 'General\'s Orders', part: '1', affects_self: 'FALSE', affects_others: 'up to 2 other friendly figures on the map', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'General Weiss', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Epic Arsenal p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Weiss', ability: 'Epic Arsenal', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Weiss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Weiss', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Weiss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Weiss', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Weiss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge (2 surges): Blast 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Weiss', ability: 'Surge (2 surges): Blast 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Weiss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Massive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'General Weiss', ability: 'Massive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'General Weiss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Get Behind Me! — condition traces', () => {
  it('Get Behind Me! p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Get Behind Me!', ability: 'Get Behind Me!', part: '1', affects_self: 'TRUE', affects_others: 'a friendly small figure within 3', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Gideon Argus — condition traces', () => {
  it('Tactical Maneuver p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Gideon Argus', ability: 'Tactical Maneuver', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure in your line of sight', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Gideon Argus', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('On My Mark p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Gideon Argus', ability: 'On My Mark', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure in your line of sight', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Gideon Argus', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gideon Argus', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gideon Argus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Gideon Argus', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Gideon Argus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Glory of the Kill — condition traces', () => {
  it('Glory of the Kill p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Glory of the Kill', ability: 'Glory of the Kill', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Greedo — condition traces', () => {
  it('Slow on the Draw p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Greedo', ability: 'Slow on the Draw', part: '1', affects_self: 'TRUE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Greedo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Parting Shot p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Greedo', ability: 'Parting Shot', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Greedo', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Greedo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Greedo', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Greedo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Greedo', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Greedo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Greedo', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Greedo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Greedo', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Greedo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Grenadier — condition traces', () => {
  it('Grenadier p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Grenadier', ability: 'Grenadier', part: '1', affects_self: 'FALSE', affects_others: 'each figure on or adjacent to the chosen space', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Grenadier', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Grisly Contest — condition traces', () => {
  it('Grisly Contest p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Grisly Contest', ability: 'Grisly Contest', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Grisly Contest', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Guardian Stance — condition traces', () => {
  it('Guardian Stance p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Guardian Stance', ability: 'Guardian Stance', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Guerilla Warfare — condition traces', () => {
  it('Guerilla Warfare p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Guerilla Warfare', ability: 'Guerilla Warfare', part: '1', affects_self: 'FALSE', affects_others: 'each friendly figure with no other friendly figures within 2 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Guild Programming — condition traces', () => {
  it('Guild Programming p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Guild Programming', ability: 'Guild Programming', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('HK Assassin Droid (Elite) — condition traces', () => {
  it('Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'HK Assassin Droid (Elite)', ability: 'Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'HK Assassin Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Versatile Weaponry p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'HK Assassin Droid (Elite)', ability: 'Versatile Weaponry', part: '1', affects_self: 'FALSE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'HK Assassin Droid (Elite)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Merciless p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'HK Assassin Droid (Elite)', ability: 'Merciless', part: '1', affects_self: 'FALSE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Priority Target p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'HK Assassin Droid (Elite)', ability: 'Priority Target', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'HK Assassin Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'HK Assassin Droid (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'HK Assassin Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'HK Assassin Droid (Elite)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'HK Assassin Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'HK Assassin Droid (Elite)', ability: 'Surge: +2 Accuracy, the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'HK Assassin Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('HK-47 — condition traces', () => {
  it('Query p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'HK-47', ability: 'Query', part: '1', affects_self: 'FALSE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'HK-47', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Conclusion p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'HK-47', ability: 'Conclusion', part: '1', affects_self: 'FALSE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'HK-47', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Mockery p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'HK-47', ability: 'Mockery', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure in your line of sight', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'HK-47', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'HK-47', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'HK-47', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'HK-47', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'HK-47', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Han Solo — condition traces', () => {
  it('Return Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Han Solo', ability: 'Return Fire', part: '1', affects_self: 'TRUE', affects_others: 'that attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Distracting p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Han Solo', ability: 'Distracting', part: '1', affects_self: 'FALSE', affects_others: 'a friendly figure being targeted', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Cunning p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Han Solo', ability: 'Cunning', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Accuracy, +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Han Solo', ability: 'Surge: +2 Accuracy, +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Han Solo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Han Solo', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Han Solo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hard to Hit — condition traces', () => {
  it('Hard to Hit p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hard to Hit', ability: 'Hard to Hit', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Hard to Hit', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'hard_to_hit-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Harsh Environment — condition traces', () => {
  it('Harsh Environment p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Harsh Environment', ability: 'Harsh Environment', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Harsh Environment', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Harsh Environment p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Harsh Environment', ability: 'Harsh Environment', part: '2', affects_self: 'TRUE', affects_others: 'all figures', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Heart of Freedom — condition traces', () => {
  it('Heart of Freedom p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heart of Freedom', ability: 'Heart of Freedom', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heart of Freedom', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Heavy Armor — condition traces', () => {
  it('Heavy Armor p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Armor', ability: 'Heavy Armor', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Heavy Armor', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'heavy_armor-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Heavy Ordnance — condition traces', () => {
  it('Heavy Ordnance p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Ordnance', ability: 'Heavy Ordnance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Ordnance', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Heavy Stormtrooper (Elite) — condition traces', () => {
  it('Modular p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Elite)', ability: 'Modular', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Spray Fire p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Elite)', ability: 'Spray Fire', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Elite)', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Elite)', ability: 'Surge: Blast 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Heavy Stormtrooper (Regular) — condition traces', () => {
  it('Composite Plating p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Heavy Stormtrooper (Regular)', ability: 'Composite Plating', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Regular)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Heavy Stormtrooper (Regular)', ability: 'Surge: Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Heavy Stormtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Heightened Reflexes — condition traces', () => {
  it('Heightened Reflexes p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Heightened Reflexes', ability: 'Heightened Reflexes', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Hera Syndulla — condition traces', () => {
  it('Call the Shots p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Hera Syndulla', ability: 'Call the Shots', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 3', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Smooth Landing p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hera Syndulla', ability: 'Smooth Landing', part: '1', affects_self: 'TRUE', affects_others: 'you and each adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hera Syndulla', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hera Syndulla', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hera Syndulla', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge (2 surges): Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hera Syndulla', ability: 'Surge (2 surges): Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hera Syndulla', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hidden Trap — condition traces', () => {
  it('Hidden Trap p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Hidden Trap', ability: 'Hidden Trap', part: '1', affects_self: 'FALSE', affects_others: 'each figure adjacent to the chosen terminal', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Hidden Trap', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Hide in Plain Sight — condition traces', () => {
  it('Hide in Plain Sight p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hide in Plain Sight', ability: 'Hide in Plain Sight', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hide in Plain Sight', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hired Gun (Elite) — condition traces', () => {
  it('Self-Preservation p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hired Gun (Elite)', ability: 'Self-Preservation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hired Gun (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Parting Shot p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Hired Gun (Elite)', ability: 'Parting Shot', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hired Gun (Elite)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hired Gun (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hired Gun (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hired Gun (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hired Gun (Regular) — condition traces', () => {
  it('Parting Shot p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Hired Gun (Regular)', ability: 'Parting Shot', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Disposable p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hired Gun (Regular)', ability: 'Disposable', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Hired Gun (Regular)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'hired_gun_regular-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hired Gun (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hired Gun (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hired Gun (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hired Gun (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hired Gun (Regular)', ability: 'Surge: +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hired Gun (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hit and Run — condition traces', () => {
  it('Hit and Run p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hit and Run', ability: 'Hit and Run', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hit and Run', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hold Ground — condition traces', () => {
  it('Hold Ground p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Hold Ground', ability: 'Hold Ground', part: '1', affects_self: 'FALSE', affects_others: 'SMALL hostile figures adjacent to you', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Hold Ground', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Hondo Ohnaka — condition traces', () => {
  it('Negotiate p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Hondo Ohnaka', ability: 'Negotiate', part: '1', affects_self: 'FALSE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Hondo Ohnaka', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('What\'s Yours is Mine p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Hondo Ohnaka', ability: 'What\'s Yours is Mine', part: '1', affects_self: 'TRUE', affects_others: 'an opponent', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hondo Ohnaka', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hondo Ohnaka', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hondo Ohnaka', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hondo Ohnaka', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Honoring the Fallen — condition traces', () => {
  it('Honoring the Fallen p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Honoring the Fallen', ability: 'Honoring the Fallen', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Hostile Negotiation — condition traces', () => {
  it('Hostile Negotiation p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hostile Negotiation', ability: 'Hostile Negotiation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hostile Negotiation', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hour of Need — condition traces', () => {
  it('Hour of Need p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hour of Need', ability: 'Hour of Need', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hour of Need', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Hunt Them Down — condition traces', () => {
  it('Hunt Them Down p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Hunt Them Down', ability: 'Hunt Them Down', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Hunter Protocol — condition traces', () => {
  it('Hunter Protocol p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Hunter Protocol', ability: 'Hunter Protocol', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Hunter Protocol', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('I Can Feel It — condition traces', () => {
  it('I Can Feel It p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'I Can Feel It', ability: 'I Can Feel It', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'I Can Feel It', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'i_can_feel_it-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('I Can Feel It p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'I Can Feel It', ability: 'I Can Feel It', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'I Can Feel It', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('I Can Feel It p3: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'I Can Feel It', ability: 'I Can Feel It', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'I Can Feel It', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('I Make My Own Luck — condition traces', () => {
  it('I Make My Own Luck p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'I Make My Own Luck', ability: 'I Make My Own Luck', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'I Make My Own Luck', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('I Must Go Alone — condition traces', () => {
  it('I Must Go Alone p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'I Must Go Alone', ability: 'I Must Go Alone', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'I Must Go Alone', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('I Must Go Alone p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'I Must Go Alone', ability: 'I Must Go Alone', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('IG-11 — condition traces', () => {
  it('Rapid Fire p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-11', ability: 'Rapid Fire', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-11', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-11', ability: 'Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-11', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Self-Destruct Protocol p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'IG-11', ability: 'Self-Destruct Protocol', part: '1', affects_self: 'TRUE', affects_others: 'each figure or object adjacent to you', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-11', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'IG-11', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'ig_11-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-11', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-11', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-11', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-11', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-11', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-11', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('IG-88 — condition traces', () => {
  it('Arsenal p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-88', ability: 'Arsenal', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-88', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Relentless p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'IG-88', ability: 'Relentless', part: '1', affects_self: 'TRUE', affects_others: 'a figure within 3', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Assault p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-88', ability: 'Assault', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-88', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-88', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-88', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-88', ability: 'Surge: Recover 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-88', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'IG-88', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'IG-88', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('ISB Infiltrator (Elite) — condition traces', () => {
  it('In The Shadows p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Elite)', ability: 'In The Shadows', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('In The Shadows p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Elite)', ability: 'In The Shadows', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Comms Jammer p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Elite)', ability: 'Comms Jammer', part: '1', affects_self: 'FALSE', affects_others: 'your opponent', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'ISB Infiltrator (Elite)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Coordinated Raid p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'ISB Infiltrator (Elite)', ability: 'Coordinated Raid', part: '1', affects_self: 'FALSE', affects_others: 'another friendly IMPERIAL figure with cost 4 or less within 4', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1, +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Elite)', ability: 'Surge: Pierce 1, +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('ISB Infiltrator (Regular) — condition traces', () => {
  it('Coordinated Raid p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'ISB Infiltrator (Regular)', ability: 'Coordinated Raid', part: '1', affects_self: 'FALSE', affects_others: 'another figure in your group', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Regular)', ability: 'Surge: you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'ISB Infiltrator (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'ISB Infiltrator (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Iden Versio — condition traces', () => {
  it('Droid Kit p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Iden Versio', ability: 'Droid Kit', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Pulse Cannon p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Iden Versio', ability: 'Pulse Cannon', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('ID10 Seeker Droid p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Iden Versio', ability: 'ID10 Seeker Droid', part: '1', affects_self: 'TRUE', affects_others: 'the Dio companion', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Iden Versio', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Iden Versio', ability: 'Surge: +2 Accuracy, +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Iden Versio', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Iden Versio', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Iden Versio', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Imperial Officer (Elite) — condition traces', () => {
  it('Executive Order p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Elite)', ability: 'Executive Order', part: '1', affects_self: 'FALSE', affects_others: 'another friendly Imperial figure within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Imperial Officer (Elite)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Elite)', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Imperial Officer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Imperial Officer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Elite)', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Imperial Officer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Elite)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Imperial Officer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Imperial Officer (Regular) — condition traces', () => {
  it('Order p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Regular)', ability: 'Order', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Imperial Officer (Regular)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Cower p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Imperial Officer (Regular)', ability: 'Cower', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Regular)', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Imperial Officer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Surge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Regular)', ability: 'Surge: +1 Surge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Imperial Officer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Imperial Officer (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Imperial Officer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Improvised Weapons — condition traces', () => {
  it('Improvised Weapons p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Improvised Weapons', ability: 'Improvised Weapons', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Improvised Weapons', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('In the Shadows — condition traces', () => {
  it('In the Shadows p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'In the Shadows', ability: 'In the Shadows', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'In the Shadows', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('In the Shadows p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'In the Shadows', ability: 'In the Shadows', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Induce Rage — condition traces', () => {
  it('Induce Rage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Induce Rage', ability: 'Induce Rage', part: '1', affects_self: 'TRUE', affects_others: 'up to 2 figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Induce Rage', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Inspiring Speech — condition traces', () => {
  it('Inspiring Speech p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Inspiring Speech', ability: 'Inspiring Speech', part: '1', affects_self: 'FALSE', affects_others: 'up to 2 friendly figures adjacent to you', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Inspiring Speech', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Intelligence Leak — condition traces', () => {
  it('Intelligence Leak p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Intelligence Leak', ability: 'Intelligence Leak', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Intelligence Leak', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Iron Will — condition traces', () => {
  it('Iron Will p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Iron Will', ability: 'Iron Will', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('J4X-7 — condition traces', () => {
  it('Supporting Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'J4X-7', ability: 'Supporting Fire', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure attacking a figure adjacent to you', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'J4X-7', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'J4X-7', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'J4X-7', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'J4X-7', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jabba the Hutt — condition traces', () => {
  it('Bully p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jabba the Hutt', ability: 'Bully', part: '1', affects_self: 'TRUE', affects_others: 'a figure of your choice within 3', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jabba the Hutt', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Incentivize p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jabba the Hutt', ability: 'Incentivize', part: '1', affects_self: 'TRUE', affects_others: 'a SCUM figure of your choice', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jabba the Hutt', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Scheme p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jabba the Hutt', ability: 'Scheme', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jabba the Hutt', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Order Hit p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jabba the Hutt', ability: 'Order Hit', part: '1', affects_self: 'TRUE', affects_others: 'a MERC figure of your choice', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jabba the Hutt', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Nefarious Gains p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Jabba the Hutt', ability: 'Nefarious Gains', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Jarrod Kelvin — condition traces', () => {
  it('Leaping Slash p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jarrod Kelvin', ability: 'Leaping Slash', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jarrod Kelvin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Droid Master p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jarrod Kelvin', ability: 'Droid Master', part: '1', affects_self: 'TRUE', affects_others: 'J4X-7', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jarrod Kelvin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jarrod Kelvin', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jarrod Kelvin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jarrod Kelvin', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Jarrod Kelvin', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'jarrod_kelvin-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jarrod Kelvin', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jarrod Kelvin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jarrod Kelvin', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jarrod Kelvin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Power token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jarrod Kelvin', ability: 'Surge: gain 1 Power token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jarrod Kelvin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jawa Scavenger (Elite) — condition traces', () => {
  it('Take Cover p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Elite)', ability: 'Take Cover', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Jawa Scavenger (Elite)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'jawa_scavenger_elite-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Bargain p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Elite)', ability: 'Bargain', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Elite)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Elite)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jawa Scavenger (Regular) — condition traces', () => {
  it('Take Cover p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Regular)', ability: 'Take Cover', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Jawa Scavenger (Regular)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'jawa_scavenger_regular-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Harass p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Jawa Scavenger (Regular)', ability: 'Harass', part: '1', affects_self: 'FALSE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Regular)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Regular)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jawa Scavenger (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jawa Scavenger (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jet Trooper (Elite) — condition traces', () => {
  it('Agile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Elite)', ability: 'Agile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Jet Trooper (Elite)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'jet_trooper_elite-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Fly-By p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Jet Trooper (Elite)', ability: 'Fly-By', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Fly-By p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Elite)', ability: 'Fly-By', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jet Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jet Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jet Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Elite)', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jet Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jet Trooper (Regular) — condition traces', () => {
  it('Agile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Regular)', ability: 'Agile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Jet Trooper (Regular)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'jet_trooper_regular-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Jets p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Jet Trooper (Regular)', ability: 'Jets', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jet Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Regular)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jet Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jet Trooper (Regular)', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jet Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jump Jets — condition traces', () => {
  it('Jump Jets p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jump Jets', ability: 'Jump Jets', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jump Jets', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jundland Terror — condition traces', () => {
  it('Jundland Terror p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jundland Terror', ability: 'Jundland Terror', part: '1', affects_self: 'TRUE', affects_others: 'a chosen "Tusken Raider" or "Bantha Rider" figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jundland Terror', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Junk Droid — condition traces', () => {
  it('Junk Droid p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Junk Droid', ability: 'Junk Droid', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Junk Droid', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Junk Droid', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Junk Droid', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Just Business — condition traces', () => {
  it('Just Business p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Just Business', ability: 'Just Business', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Just Business', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Just Business p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Just Business', ability: 'Just Business', part: '2', affects_self: 'TRUE', affects_others: 'friendly Scum figures within 3 spaces', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Jyn Erso — condition traces', () => {
  it('Trust Goes Both Ways p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Jyn Erso', ability: 'Trust Goes Both Ways', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Trust Goes Both Ways p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Jyn Erso', ability: 'Trust Goes Both Ways', part: '2', affects_self: 'TRUE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Tonfa Strike p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Erso', ability: 'Tonfa Strike', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Erso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Erso', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Erso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy, +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Erso', ability: 'Surge: +1 Accuracy, +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Erso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Erso', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Erso', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Jyn Odan — condition traces', () => {
  it('Hair Trigger p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Jyn Odan', ability: 'Hair Trigger', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure (the one activating)', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Sidewinder p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Odan', ability: 'Sidewinder', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Odan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cunning p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Odan', ability: 'Cunning', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Jyn Odan', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'jyn_odan-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Odan', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Odan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Odan', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Odan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Odan', ability: 'Surge: the target becomes Stunned, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Odan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Jyn Odan', ability: 'Surge: +2 Accuracy, +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Jyn Odan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('K-2S0 — condition traces', () => {
  it('Vague and Unconvincing p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'K-2S0', ability: 'Vague and Unconvincing', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'K-2S0', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'k_2s0-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Cassian Said I Had To p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'K-2S0', ability: 'Cassian Said I Had To', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Continually Unexpected p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'K-2S0', ability: 'Continually Unexpected', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'K-2S0', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'K-2S0', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'K-2S0', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'K-2S0', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'K-2S0', ability: 'Surge: +1 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'K-2S0', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('KX-Series Security Droid (Elite) — condition traces', () => {
  it('Shoulder Rush p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'KX-Series Security Droid (Elite)', ability: 'Shoulder Rush', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'KX-Series Security Droid (Elite)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Deference Protocol p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'KX-Series Security Droid (Elite)', ability: 'Deference Protocol', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'KX-Series Security Droid (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'KX-Series Security Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'KX-Series Security Droid (Elite)', ability: 'Surge: the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'KX-Series Security Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Kanan Jarrus — condition traces', () => {
  it('Force Vision p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Kanan Jarrus', ability: 'Force Vision', part: '1', affects_self: 'FALSE', affects_others: 'an opponent ready group', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Kanan Jarrus', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Soresu Form p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Kanan Jarrus', ability: 'Soresu Form', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 3 spaces', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kanan Jarrus', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kanan Jarrus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kanan Jarrus', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kanan Jarrus', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Karabast! — condition traces', () => {
  it('Karabast! p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Karabast!', ability: 'Karabast!', part: '1', affects_self: 'FALSE', affects_others: 'hostile figures within 2 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Kayn Somos — condition traces', () => {
  it('Firing Squad p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Kayn Somos', ability: 'Firing Squad', part: '1', affects_self: 'FALSE', affects_others: 'up to 2 adjacent friendly TROOPERS', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Kayn Somos', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Squad Command p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Kayn Somos', ability: 'Squad Command', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly TROOPER', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kayn Somos', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kayn Somos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kayn Somos', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kayn Somos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Knowledge and Defense — condition traces', () => {
  it('Knowledge and Defense p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Knowledge and Defense', ability: 'Knowledge and Defense', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Knowledge and Defense', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'knowledge_and_defense-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Passive p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Knowledge and Defense', ability: 'Passive', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Ko-Tun Feralo — condition traces', () => {
  it('Arms Distribution p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ko-Tun Feralo', ability: 'Arms Distribution', part: '1', affects_self: 'TRUE', affects_others: 'up to 2 friendly figures within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ko-Tun Feralo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Dead Precise p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Ko-Tun Feralo', ability: 'Dead Precise', part: '1', affects_self: 'TRUE', affects_others: 'a figure within 3 spaces of Ko-Tun', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Squad Cohesion p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Ko-Tun Feralo', ability: 'Squad Cohesion', part: '1', affects_self: 'TRUE', affects_others: 'a friendly REBEL figure within 3 spaces', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ko-Tun Feralo', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ko-Tun Feralo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ko-Tun Feralo', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ko-Tun Feralo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ko-Tun Feralo', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ko-Tun Feralo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ko-Tun Feralo', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ko-Tun Feralo', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Krrsantan — condition traces', () => {
  it('Full of Rage p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Krrsantan', ability: 'Full of Rage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Electrified Knuckledusters p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Krrsantan', ability: 'Electrified Knuckledusters', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Krrsantan', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Bleed p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Krrsantan', ability: 'Bleed', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Krrsantan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Krrsantan', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Krrsantan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Krrsantan', ability: 'Surge: +2 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Krrsantan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Krrsantan', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Krrsantan', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Kuiil — condition traces', () => {
  it('Mounted p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kuiil', ability: 'Mounted', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kuiil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Hop On! p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kuiil', ability: 'Hop On!', part: '1', affects_self: 'TRUE', affects_others: 'a SMALL friendly figure with cost 8 or less', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kuiil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kuiil', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kuiil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kuiil', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kuiil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kuiil', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kuiil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Kuiil', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Kuiil', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Lando Calrissian — condition traces', () => {
  it('Resourceful p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Lando Calrissian', ability: 'Resourceful', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Lando Calrissian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Resourceful p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Lando Calrissian', ability: 'Resourceful', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Lando Calrissian', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'lando_calrissian-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Gambit p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Lando Calrissian', ability: 'Gambit', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Gambit p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Lando Calrissian', ability: 'Gambit', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Shrewd Scoundrel p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Lando Calrissian', ability: 'Shrewd Scoundrel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Shrewd Scoundrel p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Lando Calrissian', ability: 'Shrewd Scoundrel', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Lando Calrissian', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Lando Calrissian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Lando Calrissian', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Lando Calrissian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1, you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Lando Calrissian', ability: 'Surge: Pierce 1, you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Lando Calrissian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Learn by Example — condition traces', () => {
  it('Learn by Example p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Learn by Example', ability: 'Learn by Example', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Leia Organa — condition traces', () => {
  it('Battlefield Leadership p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Leia Organa', ability: 'Battlefield Leadership', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Leia Organa', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Military Efficiency p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Leia Organa', ability: 'Military Efficiency', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Leia Organa', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Leia Organa', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Leia Organa', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'leia_organa-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Leia Organa', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Leia Organa', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Leia Organa', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Leia Organa', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Leia Organa', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Leia Organa', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Let\'s Make a Deal — condition traces', () => {
  it('Let\'s Make a Deal p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Let\'s Make a Deal', ability: 'Let\'s Make a Deal', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Lightbow — condition traces', () => {
  it('Lightbow p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Lightbow', ability: 'Lightbow', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Lightbow', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Lock On — condition traces', () => {
  it('Lock On p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Lock On', ability: 'Lock On', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Lock On', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Loku Kanoloa — condition traces', () => {
  it('Set Your Sights p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Loku Kanoloa', ability: 'Set Your Sights', part: '1', affects_self: 'FALSE', affects_others: 'a unique hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Loku Kanoloa', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Set Your Sights p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Loku Kanoloa', ability: 'Set Your Sights', part: '2', affects_self: 'TRUE', affects_others: 'a friendly figure attacking a figure with a Recon token', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Priority Target p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loku Kanoloa', ability: 'Priority Target', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loku Kanoloa', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mon Cala Special Forces p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Loku Kanoloa', ability: 'Mon Cala Special Forces', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+4 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loku Kanoloa', ability: '+4 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loku Kanoloa', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loku Kanoloa', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loku Kanoloa', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Looking for a Fight — condition traces', () => {
  it('Looking for a Fight p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Looking for a Fight', ability: 'Looking for a Fight', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent Small figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Looking for a Fight', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Lord of the Sith — condition traces', () => {
  it('Lord of the Sith p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Lord of the Sith', ability: 'Lord of the Sith', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Loth-cat (Elite) — condition traces', () => {
  it('Pounce p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Elite)', ability: 'Pounce', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Fresh Catch p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Elite)', ability: 'Fresh Catch', part: '1', affects_self: 'TRUE', affects_others: 'you or an adjacent CREATURE', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Curious p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Loth-cat (Elite)', ability: 'Curious', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Elite)', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Loth-cat (Regular) — condition traces', () => {
  it('Pounce p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Regular)', ability: 'Pounce', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Rat Catcher p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Regular)', ability: 'Rat Catcher', part: '1', affects_self: 'TRUE', affects_others: 'you or an adjacent CREATURE', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Curious p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Loth-cat (Regular)', ability: 'Curious', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Regular)', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Loth-cat (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Loth-cat (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Luke Skywalker — condition traces', () => {
  it('Saber Strike p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker', ability: 'Saber Strike', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Inspiring p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Luke Skywalker', ability: 'Inspiring', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 3 spaces', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Luke Skywalker', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'luke_skywalker-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Luke Skywalker (Jedi Knight) — condition traces', () => {
  it('Deflect p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Luke Skywalker (Jedi Knight)', ability: 'Deflect', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure of your choice in your line of sight', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Heroic p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker (Jedi Knight)', ability: 'Heroic', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker (Jedi Knight)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker (Jedi Knight)', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker (Jedi Knight)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker (Jedi Knight)', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Luke Skywalker (Jedi Knight)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'luke_skywalker_jedi_knight-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker (Jedi Knight)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker (Jedi Knight)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Luke Skywalker (Jedi Knight)', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Luke Skywalker (Jedi Knight)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Lure of the Dark Side — condition traces', () => {
  it('Lure of the Dark Side p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Lure of the Dark Side', ability: 'Lure of the Dark Side', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure in your line of sight', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Lure of the Dark Side', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('MHD-19 — condition traces', () => {
  it('Medical Loadout p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'MHD-19', ability: 'Medical Loadout', part: '1', affects_self: 'TRUE', affects_others: 'you or an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'MHD-19', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Improper Procedure p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'MHD-19', ability: 'Improper Procedure', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'MHD-19', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'MHD-19', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'MHD-19', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'MHD-19', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'MHD-19', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'MHD-19', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'MHD-19', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Mak Eshka\'rey — condition traces', () => {
  it('Camouflage p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Mak Eshka\'rey', ability: 'Camouflage', part: '1', affects_self: 'FALSE', affects_others: 'hostile figures 4 or more spaces away', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Mak Eshka\'rey', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Critical Hit p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mak Eshka\'rey', ability: 'Critical Hit', part: '1', affects_self: 'TRUE', affects_others: 'the target figure', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mak Eshka\'rey', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Priority Target p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mak Eshka\'rey', ability: 'Priority Target', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mak Eshka\'rey', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mak Eshka\'rey', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mak Eshka\'rey', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mak Eshka\'rey', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mak Eshka\'rey', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Mandalorian Steel — condition traces', () => {
  it('Mandalorian Steel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mandalorian Steel', ability: 'Mandalorian Steel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mandalorian Steel', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mandalorian Steel p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Mandalorian Steel', ability: 'Mandalorian Steel', part: '2', affects_self: 'TRUE', affects_others: 'a friendly figure within 4 spaces', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Mandalorian Tactics — condition traces', () => {
  it('Mandalorian Tactics p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mandalorian Tactics', ability: 'Mandalorian Tactics', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mandalorian Tactics', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Mara Jade — condition traces', () => {
  it('Adaptive Skills p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mara Jade', ability: 'Adaptive Skills', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mara Jade', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Fast Learner p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Mara Jade', ability: 'Fast Learner', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mara Jade', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mara Jade', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mara Jade', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mara Jade', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mara Jade', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mara Jade', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Marked Territory — condition traces', () => {
  it('Marked Territory p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Marked Territory', ability: 'Marked Territory', part: '1', affects_self: 'TRUE', affects_others: 'a figure in your group in an exterior space', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Marked Territory', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Marksman — condition traces', () => {
  it('Marksman p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Marksman', ability: 'Marksman', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Master Operative — condition traces', () => {
  it('Master Operative p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Master Operative', ability: 'Master Operative', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Maul — condition traces', () => {
  it('Dual-Bladed Fury p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Maul', ability: 'Dual-Bladed Fury', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Maul', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Sustained by Rage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Maul', ability: 'Sustained by Rage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Maul', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Sustained by Rage p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Maul', ability: 'Sustained by Rage', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Stalk Prey p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Maul', ability: 'Stalk Prey', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Maul', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Maul', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Maul', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Maul', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Maul', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Maximum Firepower — condition traces', () => {
  it('Maximum Firepower p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Maximum Firepower', ability: 'Maximum Firepower', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Maximum Firepower', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Meditation — condition traces', () => {
  it('Meditation p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Meditation', ability: 'Meditation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Meditation', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Merciless — condition traces', () => {
  it('Merciless p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Merciless', ability: 'Merciless', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Migs Mayfeld — condition traces', () => {
  it('Locked and Loaded p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Migs Mayfeld', ability: 'Locked and Loaded', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Migs Mayfeld', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Droid Arm p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Migs Mayfeld', ability: 'Droid Arm', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Migs Mayfeld', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Return Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Migs Mayfeld', ability: 'Return Fire', part: '1', affects_self: 'FALSE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Migs Mayfeld', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Migs Mayfeld', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Migs Mayfeld', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Migs Mayfeld', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy, +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Migs Mayfeld', ability: 'Surge: +1 Accuracy, +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Migs Mayfeld', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Miracle Worker — condition traces', () => {
  it('Miracle Worker p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Miracle Worker', ability: 'Miracle Worker', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 3 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Mitigate — condition traces', () => {
  it('Mitigate p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Mitigate', ability: 'Mitigate', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Mitigate', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Moff Gideon — condition traces', () => {
  it('I Know Everything p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Moff Gideon', ability: 'I Know Everything', part: '1', affects_self: 'FALSE', affects_others: 'opponent Command deck', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Moff Gideon', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('You Have Something I Want p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Moff Gideon', ability: 'You Have Something I Want', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 4 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Moff Gideon', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Moff Gideon', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'moff_gideon-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Moff Gideon', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Moff Gideon', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Moff Gideon', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Moff Gideon', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Murne Rin — condition traces', () => {
  it('False Orders p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Murne Rin', ability: 'False Orders', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure with cost 4 or less within 4 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Murne Rin', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Field Report p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Murne Rin', ability: 'Field Report', part: '1', affects_self: 'TRUE', affects_others: 'up to 2 friendly figures within 4 spaces with 2 or less attack dice', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Murne Rin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Figurehead p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Murne Rin', ability: 'Figurehead', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 4 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Murne Rin', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Murne Rin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Murne Rin', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Murne Rin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Murne Rin', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Murne Rin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Murne Rin', ability: 'Surge: you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Murne Rin', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Navigation Upgrade — condition traces', () => {
  it('Navigation Upgrade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Navigation Upgrade', ability: 'Navigation Upgrade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Navigation Upgrade', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Navigation Upgrade p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Navigation Upgrade', ability: 'Navigation Upgrade', part: '2', affects_self: 'TRUE', affects_others: 'a friendly DROID', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Negation — condition traces', () => {
  it('Negation p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Negation', ability: 'Negation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('New Orders — condition traces', () => {
  it('New Orders p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'New Orders', ability: 'New Orders', part: '1', affects_self: 'FALSE', affects_others: '1 adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Nexu (Elite) — condition traces', () => {
  it('Pounce p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Elite)', ability: 'Pounce', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cunning p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Elite)', ability: 'Cunning', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Nexu (Elite)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'nexu_elite-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Non-Sentient p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Elite)', ability: 'Non-Sentient', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Bleed p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Elite)', ability: 'Bleed', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Elite)', ability: 'Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Elite)', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Nexu (Regular) — condition traces', () => {
  it('Pounce p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Regular)', ability: 'Pounce', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Cunning p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Regular)', ability: 'Cunning', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Nexu (Regular)', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'nexu_regular-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Non-Sentient p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Regular)', ability: 'Non-Sentient', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Bleed p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Regular)', ability: 'Bleed', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Regular)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Regular)', ability: 'Surge: Cleave 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Nexu (Regular)', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Nexu (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Obi-Wan Kenobi — condition traces', () => {
  it('Alter Mind p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Obi-Wan Kenobi', ability: 'Alter Mind', part: '1', affects_self: 'FALSE', affects_others: 'hostile figures with cost 9 or less within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Obi-Wan Kenobi', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Strike Me Down p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Obi-Wan Kenobi', ability: 'Strike Me Down', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Into the Force p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Obi-Wan Kenobi', ability: 'Into the Force', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Obi-Wan Kenobi', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Obi-Wan Kenobi', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Obi-Wan Kenobi', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'obi_wan_kenobi-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Obi-Wan Kenobi', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Obi-Wan Kenobi', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Obi-Wan Kenobi', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Obi-Wan Kenobi', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Of No Importance — condition traces', () => {
  it('Of No Importance p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Of No Importance', ability: 'Of No Importance', part: '1', affects_self: 'TRUE', affects_others: 'one of your non-unique figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Officer\'s Training — condition traces', () => {
  it('Officer\'s Training p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Officer\'s Training', ability: 'Officer\'s Training', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Officer\'s Training', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Officer\'s Training p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Officer\'s Training', ability: 'Officer\'s Training', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('On a Mission — condition traces', () => {
  it('On a Mission p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'On a Mission', ability: 'On a Mission', part: '1', affects_self: 'TRUE', affects_others: 'SMALL figures you enter the space of', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'On a Mission', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('On the Lam — condition traces', () => {
  it('On the Lam p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'On the Lam', ability: 'On the Lam', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Onar Koma — condition traces', () => {
  it('Rush p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Onar Koma', ability: 'Rush', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent SMALL figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Onar Koma', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Get Down p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Onar Koma', ability: 'Get Down', part: '1', affects_self: 'TRUE', affects_others: 'a small figure within 2 spaces', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Immune p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Onar Koma', ability: 'Immune', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Onar Koma', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage, -2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Onar Koma', ability: 'Surge: +2 Damage, -2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Onar Koma', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, -1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Onar Koma', ability: 'Surge: +1 Damage, -1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Onar Koma', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('One in a Million — condition traces', () => {
  it('One in a Million p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'One in a Million', ability: 'One in a Million', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Opportunistic — condition traces', () => {
  it('Opportunistic p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Opportunistic', ability: 'Opportunistic', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Optimal Bombardment — condition traces', () => {
  it('Optimal Bombardment p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Optimal Bombardment', ability: 'Optimal Bombardment', part: '1', affects_self: 'FALSE', affects_others: 'up to 3 VEHICLES DROIDS or HEAVY WEAPONS adjacent to you', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Out of Time — condition traces', () => {
  it('Out of Time p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Out of Time', ability: 'Out of Time', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 3 spaces and line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Overcharged Weapons — condition traces', () => {
  it('Overcharged Weapons p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Overcharged Weapons', ability: 'Overcharged Weapons', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Overdrive — condition traces', () => {
  it('Overdrive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Overdrive', ability: 'Overdrive', part: '1', affects_self: 'TRUE', affects_others: 'each of your DROIDS', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Overdrive', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Overheated — condition traces', () => {
  it('Overheated p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Overheated', ability: 'Overheated', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Overrun — condition traces', () => {
  it('Overrun p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Overrun', ability: 'Overrun', part: '1', affects_self: 'FALSE', affects_others: 'hostile figures whose space you enter', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Overwhelming Impact — condition traces', () => {
  it('Overwhelming Impact p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Overwhelming Impact', ability: 'Overwhelming Impact', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Overwhelming Impact', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Pack Alpha — condition traces', () => {
  it('Pack Alpha p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Pack Alpha', ability: 'Pack Alpha', part: '1', affects_self: 'TRUE', affects_others: 'up to 3 friendly CREATURES within 3 spaces and a chosen hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Pack Alpha', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Paid in Beskar — condition traces', () => {
  it('Paid in Beskar p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Paid in Beskar', ability: 'Paid in Beskar', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Parry — condition traces', () => {
  it('Parry p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Parry', ability: 'Parry', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Parry', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'parry-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Parting Blow — condition traces', () => {
  it('Parting Blow p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Parting Blow', ability: 'Parting Blow', part: '1', affects_self: 'TRUE', affects_others: 'the hostile figure exiting an adjacent space', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Payback — condition traces', () => {
  it('Payback p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Payback', ability: 'Payback', part: '1', affects_self: 'TRUE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Paz Vizsla — condition traces', () => {
  it('Heavy Repeater p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Paz Vizsla', ability: 'Heavy Repeater', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Submit or Fight p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Paz Vizsla', ability: 'Submit or Fight', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: gain 1 Block token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Paz Vizsla', ability: 'Surge: gain 1 Block token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Paz Vizsla', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Paz Vizsla', ability: 'Surge: +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Paz Vizsla', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Personal Energy Shield — condition traces', () => {
  it('Personal Energy Shield p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Personal Energy Shield', ability: 'Personal Energy Shield', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Personal Energy Shield', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Personal Energy Shield p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Personal Energy Shield', ability: 'Personal Energy Shield', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Pickpocket — condition traces', () => {
  it('Pickpocket p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Pickpocket', ability: 'Pickpocket', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Pit Droid — condition traces', () => {
  it('Useful p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Pit Droid', ability: 'Useful', part: '1', affects_self: 'TRUE', affects_others: 'adjacent heroes', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Pit Droid', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Shiny p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Pit Droid', ability: 'Shiny', part: '1', affects_self: 'TRUE', affects_others: 'heroes', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Planning — condition traces', () => {
  it('Planning p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Planning', ability: 'Planning', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Planning', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Positioning Advantage — condition traces', () => {
  it('Positioning Advantage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Positioning Advantage', ability: 'Positioning Advantage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Positioning Advantage', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Prepared for Battle — condition traces', () => {
  it('Prepared for Battle p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Prepared for Battle', ability: 'Prepared for Battle', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Preservation Protocol — condition traces', () => {
  it('Preservation Protocol p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Preservation Protocol', ability: 'Preservation Protocol', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Price of Glory — condition traces', () => {
  it('Price of Glory p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Price of Glory', ability: 'Price of Glory', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Price of Glory', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Price on Their Heads — condition traces', () => {
  it('Price on Their Heads p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Price on Their Heads', ability: 'Price on Their Heads', part: '1', affects_self: 'FALSE', affects_others: 'a hostile Deployment card group', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Price on Their Heads', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Price on Their Heads p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Price on Their Heads', ability: 'Price on Their Heads', part: '2', affects_self: 'TRUE', affects_others: 'the marked hostile group', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Primary Target — condition traces', () => {
  it('Primary Target p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Primary Target', ability: 'Primary Target', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Probe Droid (Elite) — condition traces', () => {
  it('Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Elite)', ability: 'Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Self-Destruct p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Elite)', ability: 'Self-Destruct', part: '1', affects_self: 'TRUE', affects_others: 'each adjacent figure or object', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Elite)', ability: 'Surge: Recover 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Elite)', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Probe Droid (Regular) — condition traces', () => {
  it('Self-Destruct p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Regular)', ability: 'Self-Destruct', part: '1', affects_self: 'TRUE', affects_others: 'each adjacent figure and object', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Regular)', ability: 'Surge: Recover 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Probe Droid (Regular)', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Probe Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Protect the Old Ways — condition traces', () => {
  it('Protect the Old Ways p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Protect the Old Ways', ability: 'Protect the Old Ways', part: '1', affects_self: 'TRUE', affects_others: 'a figure within 3 spaces that is defending', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Provoke — condition traces', () => {
  it('Provoke p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Provoke', ability: 'Provoke', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure adjacent to one of your TROOPERS or GUARDIANS', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Pummel — condition traces', () => {
  it('Pummel p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Pummel', ability: 'Pummel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Purge Commander (Elite) — condition traces', () => {
  it('Shock Grenade p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Purge Commander (Elite)', ability: 'Shock Grenade', part: '1', affects_self: 'FALSE', affects_others: 'each other figure on or adjacent to chosen space within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Coordinated Hunt p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Purge Commander (Elite)', ability: 'Coordinated Hunt', part: '1', affects_self: 'TRUE', affects_others: 'a friendly HUNTER in your line of sight', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Purge Commander (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Purge Commander (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Purge Commander (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Purge Commander (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Purge Trooper (Elite) — condition traces', () => {
  it('Imperial Loadout p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Purge Trooper (Elite)', ability: 'Imperial Loadout', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Purge Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('On the Hunt p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Purge Trooper (Elite)', ability: 'On the Hunt', part: '1', affects_self: 'TRUE', affects_others: 'a unique hostile figure (attack target)', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('R2-D2 — condition traces', () => {
  it('Scomp Link p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'R2-D2', ability: 'Scomp Link', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Service p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'R2-D2', ability: 'Service', part: '1', affects_self: 'TRUE', affects_others: 'you or an adjacent friendly DROID or VEHICLE', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'R2-D2', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Lucky p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'R2-D2', ability: 'Lucky', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'R2-D2', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'R2-D2', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Surge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'R2-D2', ability: '+1 Surge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'R2-D2', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'R2-D2', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'R2-D2', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'R2-D2', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'R2-D2', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'R2-D2', ability: 'Surge: the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'R2-D2', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rally — condition traces', () => {
  it('Rally p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rally', ability: 'Rally', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rally', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rally the Troops — condition traces', () => {
  it('Rally the Troops p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rally the Troops', ability: 'Rally the Troops', part: '1', affects_self: 'FALSE', affects_others: 'another friendly TROOPER within 3 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Rancor — condition traces', () => {
  it('Crippling Blow p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rancor', ability: 'Crippling Blow', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rancor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Trained p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rancor', ability: 'Trained', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rancor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Voracious p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rancor', ability: 'Voracious', part: '1', affects_self: 'TRUE', affects_others: 'a friendly non-companion figure within 2', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rancor', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rancor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Block 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rancor', ability: 'Block 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Rancor', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'rancor-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rancor', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rancor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rancor', ability: 'Surge: Cleave 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rancor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Massive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rancor', ability: 'Massive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rancor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rank and File — condition traces', () => {
  it('Rank and File p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rank and File', ability: 'Rank and File', part: '1', affects_self: 'TRUE', affects_others: 'each friendly TROOPER adjacent to you', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rank and File', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rapid Recalibration — condition traces', () => {
  it('Rapid Recalibration p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rapid Recalibration', ability: 'Rapid Recalibration', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Reactive Loyalties — condition traces', () => {
  it('Reactive Loyalties p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Reactive Loyalties', ability: 'Reactive Loyalties', part: '1', affects_self: 'TRUE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Ready Weapons — condition traces', () => {
  it('Ready Weapons p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ready Weapons', ability: 'Ready Weapons', part: '1', affects_self: 'TRUE', affects_others: 'figures in your group', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ready Weapons', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rebel Graffiti — condition traces', () => {
  it('Rebel Graffiti p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rebel Graffiti', ability: 'Rebel Graffiti', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Rebel Pathfinder (Elite) — condition traces', () => {
  it('Infiltration p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Pathfinder (Elite)', ability: 'Infiltration', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Pathfinder (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Light It Up p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rebel Pathfinder (Elite)', ability: 'Light It Up', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Distracting Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rebel Pathfinder (Elite)', ability: 'Distracting Fire', part: '1', affects_self: 'TRUE', affects_others: 'the defender\'s group', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Pathfinder (Elite)', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Pathfinder (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Pathfinder (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Pathfinder (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Pathfinder (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Pathfinder (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rebel Saboteur (Elite) — condition traces', () => {
  it('Overload p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Elite)', ability: 'Overload', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Priority Target p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Elite)', ability: 'Priority Target', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+4 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Elite)', ability: '+4 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Elite)', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Elite)', ability: 'Surge: Blast 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rebel Saboteur (Regular) — condition traces', () => {
  it('Overload p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Regular)', ability: 'Overload', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Regular)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Regular)', ability: 'Surge: Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Saboteur (Regular)', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Saboteur (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rebel Trooper (Elite) — condition traces', () => {
  it('Get into Position p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Trooper (Elite)', ability: 'Get into Position', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Aim p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rebel Trooper (Elite)', ability: 'Aim', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Get Ready p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Trooper (Elite)', ability: 'Get Ready', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure in your group', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Trooper (Elite)', ability: 'Surge: +1 Damage, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Trooper (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Rebel Trooper (Regular) — condition traces', () => {
  it('Aim p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Rebel Trooper (Regular)', ability: 'Aim', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Trooper (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rebel Trooper (Regular)', ability: 'Surge: +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rebel Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Recovery — condition traces', () => {
  it('Recovery p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Recovery', ability: 'Recovery', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Recovery', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Reduce to Rubble — condition traces', () => {
  it('Reduce to Rubble p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Reduce to Rubble', ability: 'Reduce to Rubble', part: '1', affects_self: 'TRUE', affects_others: 'each figure and object within 2 spaces of the target space', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Regroup — condition traces', () => {
  it('Regroup p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Regroup', ability: 'Regroup', part: '1', affects_self: 'FALSE', affects_others: 'adjacent friendly figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Regroup', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Reinforcements — condition traces', () => {
  it('Reinforcements p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Reinforcements', ability: 'Reinforcements', part: '1', affects_self: 'TRUE', affects_others: 'one of your defeated TROOPERS with reinforcement cost 3 or less', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Repair — condition traces', () => {
  it('Repair p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Repair', ability: 'Repair', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly DROID or VEHICLE', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Repair p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Repair', ability: 'Repair', part: '2', affects_self: 'FALSE', affects_others: 'an adjacent friendly DROID or VEHICLE', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Reposition — condition traces', () => {
  it('Reposition p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Reposition', ability: 'Reposition', part: '1', affects_self: 'TRUE', affects_others: 'a SMALL friendly figure within 3 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Rest in Peace — condition traces', () => {
  it('Rest in Peace p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rest in Peace', ability: 'Rest in Peace', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rest in Peace', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Rest in Peace p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rest in Peace', ability: 'Rest in Peace', part: '2', affects_self: 'TRUE', affects_others: 'all players', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rest in Peace', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Rest in Peace p3: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Rest in Peace', ability: 'Rest in Peace', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Rest in Peace', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Retaliation — condition traces', () => {
  it('Retaliation p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Retaliation', ability: 'Retaliation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Reverse Engineer — condition traces', () => {
  it('Reverse Engineer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Reverse Engineer', ability: 'Reverse Engineer', part: '1', affects_self: 'TRUE', affects_others: 'the defender', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Reverse Engineer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Right Back At Ya! — condition traces', () => {
  it('Right Back At Ya! p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Right Back At Ya!', ability: 'Right Back At Ya!', part: '1', affects_self: 'TRUE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Riot Trooper (Elite) — condition traces', () => {
  it('Weaken p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Riot Trooper (Elite)', ability: 'Weaken', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Riot Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Stun Batons p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Riot Trooper (Elite)', ability: 'Stun Batons', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Shield p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Riot Trooper (Elite)', ability: 'Shield', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Riot Trooper (Elite)', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Riot Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Riot Trooper (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Riot Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Riot Trooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Riot Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Riot Trooper (Regular) — condition traces', () => {
  it('Stun Batons p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Riot Trooper (Regular)', ability: 'Stun Batons', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Shield p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Riot Trooper (Regular)', ability: 'Shield', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Riot Trooper (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Riot Trooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Roar — condition traces', () => {
  it('Roar p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Roar', ability: 'Roar', part: '1', affects_self: 'FALSE', affects_others: 'up to 3 adjacent hostile figures', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Royal Guard (Elite) — condition traces', () => {
  it('Sentinel p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard (Elite)', ability: 'Sentinel', part: '1', affects_self: 'FALSE', affects_others: 'a friendly non-GUARDIAN figure (the defending figure)', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Forward Vengeance p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard (Elite)', ability: 'Forward Vengeance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Elite)', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Elite)', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Elite)', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned, gain 1 Evade token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Elite)', ability: 'Surge: the target becomes Stunned, gain 1 Evade token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Royal Guard (Regular) — condition traces', () => {
  it('Sentinel p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard (Regular)', ability: 'Sentinel', part: '1', affects_self: 'FALSE', affects_others: 'a friendly non-GUARDIAN figure (the defending figure)', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Vengeance p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard (Regular)', ability: 'Vengeance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Regular)', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Regular)', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Royal Guard Champion — condition traces', () => {
  it('Brutality p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard Champion', ability: 'Brutality', part: '1', affects_self: 'TRUE', affects_others: 'two attack targets', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Executor p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard Champion', ability: 'Executor', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Overpower p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard Champion', ability: 'Overpower', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Overpower p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Royal Guard Champion', ability: 'Overpower', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard Champion', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard Champion', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard Champion', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard Champion', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard Champion', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard Champion', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Royal Guard Champion', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Royal Guard Champion', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Run for Cover — condition traces', () => {
  it('Run for Cover p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Run for Cover', ability: 'Run for Cover', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('SC2-M Repulsor Tank — condition traces', () => {
  it('Focus Fire p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'SC2-M Repulsor Tank', ability: 'Focus Fire', part: '1', affects_self: 'TRUE', affects_others: 'two attacks on the same figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'SC2-M Repulsor Tank', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Defensible p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'SC2-M Repulsor Tank', ability: 'Defensible', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'SC2-M Repulsor Tank', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'SC2-M Repulsor Tank', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'SC2-M Repulsor Tank', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'SC2-M Repulsor Tank', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'SC2-M Repulsor Tank', ability: 'Surge: Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'SC2-M Repulsor Tank', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Massive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'SC2-M Repulsor Tank', ability: 'Massive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'SC2-M Repulsor Tank', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Sabine Wren — condition traces', () => {
  it('Evasive Maneuver p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sabine Wren', ability: 'Evasive Maneuver', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sabine Wren', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Parting Gift p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Sabine Wren', ability: 'Parting Gift', part: '1', affects_self: 'FALSE', affects_others: 'each other figure and object on or adjacent to chosen space within 3', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Sabine Wren', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sabine Wren', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sabine Wren', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sabine Wren', ability: 'Surge: +1 Damage, Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sabine Wren', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sabine Wren', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sabine Wren', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Salacious B. Crumb — condition traces', () => {
  it('Swipe p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Salacious B. Crumb', ability: 'Swipe', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure in the entered space', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Scratch p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Salacious B. Crumb', ability: 'Scratch', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Salacious B. Crumb', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Sarlacc Sweep — condition traces', () => {
  it('Sarlacc Sweep p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Sarlacc Sweep', ability: 'Sarlacc Sweep', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Saska Teft — condition traces', () => {
  it('Unstable Devices p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saska Teft', ability: 'Unstable Devices', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure in your line of sight', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saska Teft', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Power Converter p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Saska Teft', ability: 'Power Converter', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure with a Device token', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saska Teft', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saska Teft', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saska Teft', ability: 'Surge: +1 Damage, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saska Teft', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage, +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saska Teft', ability: 'Surge: +2 Damage, +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saska Teft', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened, the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saska Teft', ability: 'Surge: the target becomes Weakened, the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saska Teft', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Savage Vigor — condition traces', () => {
  it('Savage Vigor p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Savage Vigor', ability: 'Savage Vigor', part: '1', affects_self: 'FALSE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Saw Gerrerra — condition traces', () => {
  it('Brutal Tactics p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Saw Gerrerra', ability: 'Brutal Tactics', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 3 spaces of the defeated figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Wanton Destruction p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Saw Gerrerra', ability: 'Wanton Destruction', part: '1', affects_self: 'FALSE', affects_others: 'up to 2 figures other than the defender within 2 spaces of the target space', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saw Gerrerra', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saw Gerrerra', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Surge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saw Gerrerra', ability: '+1 Surge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saw Gerrerra', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saw Gerrerra', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saw Gerrerra', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Saw Gerrerra', ability: 'Surge: you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Saw Gerrerra', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Scout Trooper (Elite) — condition traces', () => {
  it('Camouflage p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Scout Trooper (Elite)', ability: 'Camouflage', part: '1', affects_self: 'FALSE', affects_others: 'hostile figures 4 or more spaces away', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Scout Trooper (Elite)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Find Weakness p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Scout Trooper (Elite)', ability: 'Find Weakness', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Exploit Weakness p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Scout Trooper (Elite)', ability: 'Exploit Weakness', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Scout Trooper (Elite)', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Scout Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Scout Trooper (Elite)', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Scout Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Scout Trooper (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Scout Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Scout Trooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Scout Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1, the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Scout Trooper (Elite)', ability: 'Surge: Pierce 1, the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Scout Trooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Second Chance — condition traces', () => {
  it('Second Chance p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Second Chance', ability: 'Second Chance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Second Chance', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Second Chance p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Second Chance', ability: 'Second Chance', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Second Sister — condition traces', () => {
  it('Force Leap p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Second Sister', ability: 'Force Leap', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Second Sister', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Saber Orbit p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Second Sister', ability: 'Saber Orbit', part: '1', affects_self: 'FALSE', affects_others: 'up to 3 adjacent hostile figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Second Sister', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Mastery p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Second Sister', ability: 'Mastery', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Surge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Second Sister', ability: '+1 Surge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Second Sister', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Second Sister', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Second Sister', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Second Sister', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Second Sister', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Self-Augmentation — condition traces', () => {
  it('Self-Augmentation p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Self-Augmentation', ability: 'Self-Augmentation', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Self-Augmentation', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Self-Augmentation p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Self-Augmentation', ability: 'Self-Augmentation', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Self-Augmentation', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Self-Augmentation p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Self-Augmentation', ability: 'Self-Augmentation', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Self-Defense — condition traces', () => {
  it('Self-Defense p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Self-Defense', ability: 'Self-Defense', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure entering an adjacent space', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Sentry Droid (Elite) — condition traces', () => {
  it('Multi-Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Sentry Droid (Elite)', ability: 'Multi-Fire', part: '1', affects_self: 'TRUE', affects_others: 'two attacks with different targets', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Charged Shot p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Elite)', ability: 'Charged Shot', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Elite)', ability: 'Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Elite)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Elite)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Sentry Droid (Regular) — condition traces', () => {
  it('Multi-Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Sentry Droid (Regular)', ability: 'Multi-Fire', part: '1', affects_self: 'TRUE', affects_others: 'two attacks with different targets', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Charged Shot p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Regular)', ability: 'Charged Shot', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Regular)', ability: 'Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Regular)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sentry Droid (Regular)', ability: 'Surge: +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sentry Droid (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Set a Trap — condition traces', () => {
  it('Set a Trap p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Set a Trap', ability: 'Set a Trap', part: '1', affects_self: 'TRUE', affects_others: 'a chosen map tile', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Set a Trap', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Set a Trap p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Set a Trap', ability: 'Set a Trap', part: '2', affects_self: 'TRUE', affects_others: 'one of your figures on that tile and a hostile figure on that tile', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Set for Stun — condition traces', () => {
  it('Set for Stun p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Set for Stun', ability: 'Set for Stun', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Set for Stun', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Set the Charges — condition traces', () => {
  it('Set the Charges p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Set the Charges', ability: 'Set the Charges', part: '1', affects_self: 'TRUE', affects_others: 'each figure or object on or adjacent to the chosen space', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Set the Charges', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Shadow Ops — condition traces', () => {
  it('Shadow Ops p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Shadow Ops', ability: 'Shadow Ops', part: '1', affects_self: 'FALSE', affects_others: 'the opposing player', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Shadow Ops', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Shared Experience — condition traces', () => {
  it('Shared Experience p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shared Experience', ability: 'Shared Experience', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shared Experience', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Shared Experience p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Shared Experience', ability: 'Shared Experience', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Shoot the Messenger — condition traces', () => {
  it('Shoot the Messenger p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Shoot the Messenger', ability: 'Shoot the Messenger', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Shoretrooper (Elite) — condition traces', () => {
  it('Squad Training p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Shoretrooper (Elite)', ability: 'Squad Training', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shoretrooper (Elite)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shoretrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shoretrooper (Elite)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shoretrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shoretrooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shoretrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shoretrooper (Elite)', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shoretrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Shyla Varad — condition traces', () => {
  it('Mandalorian Whip p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Shyla Varad', ability: 'Mandalorian Whip', part: '1', affects_self: 'TRUE', affects_others: 'a SMALL hostile figure within 3 spaces and line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Responsive p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shyla Varad', ability: 'Responsive', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shyla Varad', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shyla Varad', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Shyla Varad', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'shyla_varad-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shyla Varad', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shyla Varad', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shyla Varad', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shyla Varad', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Shyla Varad', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Shyla Varad', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Signal Jammer — condition traces', () => {
  it('Signal Jammer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Signal Jammer', ability: 'Signal Jammer', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Signal Jammer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Signal Jammer p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Signal Jammer', ability: 'Signal Jammer', part: '2', affects_self: 'TRUE', affects_others: 'any player who plays a Command card', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Signal Jammer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Single Purpose — condition traces', () => {
  it('Single Purpose p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Single Purpose', ability: 'Single Purpose', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Single Purpose', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Sit Tight — condition traces', () => {
  it('Sit Tight p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Sit Tight', ability: 'Sit Tight', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Size Advantage — condition traces', () => {
  it('Size Advantage p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Size Advantage', ability: 'Size Advantage', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Slippery Target — condition traces', () => {
  it('Slippery Target p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Slippery Target', ability: 'Slippery Target', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Smoke Grenade — condition traces', () => {
  it('Smoke Grenade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Smoke Grenade', ability: 'Smoke Grenade', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 2 of the chosen space', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Smoke Grenade', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Smuggled Supplies — condition traces', () => {
  it('Smuggled Supplies p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Smuggled Supplies', ability: 'Smuggled Supplies', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Smuggled Supplies', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Smuggler\'s Tricks — condition traces', () => {
  it('Smuggler\'s Tricks p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Smuggler\'s Tricks', ability: 'Smuggler\'s Tricks', part: '1', affects_self: 'TRUE', affects_others: 'your opponent\'s figures on or adjacent to the chosen tile or token', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Smuggler\'s Tricks', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Sniper Configuration — condition traces', () => {
  it('Sniper Configuration p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Sniper Configuration', ability: 'Sniper Configuration', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Sniper Configuration', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Snowtrooper (Elite) — condition traces', () => {
  it('Disruptor Rifle p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Snowtrooper (Elite)', ability: 'Disruptor Rifle', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Spiked Boots p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Elite)', ability: 'Spiked Boots', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Immune p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Elite)', ability: 'Immune', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Elite)', ability: 'Surge: +2 Accuracy, +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Elite)', ability: 'Surge: +1 Damage, the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Elite)', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Elite)', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Snowtrooper (Regular) — condition traces', () => {
  it('Environmental Recovery Gear p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Regular)', ability: 'Environmental Recovery Gear', part: '1', affects_self: 'TRUE', affects_others: 'each adjacent friendly TROOPER', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Regular)', ability: 'Surge: the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Snowtrooper (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Snowtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Son of Skywalker — condition traces', () => {
  it('Son of Skywalker p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Son of Skywalker', ability: 'Son of Skywalker', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Son of Skywalker', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Spinning Kick — condition traces', () => {
  it('Spinning Kick p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Spinning Kick', ability: 'Spinning Kick', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Spinning Kick', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Squad Swarm — condition traces', () => {
  it('Squad Swarm p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Squad Swarm', ability: 'Squad Swarm', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Stall for Time — condition traces', () => {
  it('Stall for Time p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Stall for Time', ability: 'Stall for Time', part: '1', affects_self: 'FALSE', affects_others: 'your opponent', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Stall for Time', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Static Pulse — condition traces', () => {
  it('Static Pulse p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Static Pulse', ability: 'Static Pulse', part: '1', affects_self: 'TRUE', affects_others: 'each hostile figure adjacent to a friendly \'Dio\'', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Static Pulse', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Stay Down — condition traces', () => {
  it('Stay Down p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Stay Down', ability: 'Stay Down', part: '1', affects_self: 'TRUE', affects_others: 'the same attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Stealth Tactics — condition traces', () => {
  it('Stealth Tactics p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Stealth Tactics', ability: 'Stealth Tactics', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Stealth Tactics', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'stealth_tactics-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
});

describe('Still Faster Than You — condition traces', () => {
  it('Still Faster Than You p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Still Faster Than You', ability: 'Still Faster Than You', part: '1', affects_self: 'TRUE', affects_others: 'a different hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Still Faster Than You', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Stimulants — condition traces', () => {
  it('Stimulants p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Stimulants', ability: 'Stimulants', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Stimulants', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Stormtrooper (Elite) — condition traces', () => {
  it('Squad Training p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Stormtrooper (Elite)', ability: 'Squad Training', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Last Stand p1: owner IS activator (in_group_of_source fires; beneficiary is "another" teammate)', () => {
    // affects_self=FALSE means the owner is not the BENEFICIARY (another figure gets the reroll).
    // But the owner IS the activator — conditionForRow correctly fires via in_group_of_source
    // so the button appears when the owner attacks, offering the reroll to a teammate.
    const cond = conditionForRow({ card: 'Stormtrooper (Elite)', ability: 'Last Stand', part: '1', affects_self: 'FALSE', affects_others: 'another figure in your group', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner activates → button offered');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-group member → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Stormtrooper (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Stormtrooper (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Stormtrooper (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Stormtrooper (Regular) — condition traces', () => {
  it('Squad Training p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Stormtrooper (Regular)', ability: 'Squad Training', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Stormtrooper (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Stormtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Stormtrooper (Regular)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Stormtrooper (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Strategic Shift — condition traces', () => {
  it('Strategic Shift p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Strategic Shift', ability: 'Strategic Shift', part: '1', affects_self: 'TRUE', affects_others: 'a player of your choice', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Strategic Shift', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Strength in Numbers — condition traces', () => {
  it('Strength in Numbers p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Strength in Numbers', ability: 'Strength in Numbers', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Stroke of Brilliance — condition traces', () => {
  it('Stroke of Brilliance p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Stroke of Brilliance', ability: 'Stroke of Brilliance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Super Commando (Elite) — condition traces', () => {
  it('Jetpack Rocket p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Super Commando (Elite)', ability: 'Jetpack Rocket', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 3 spaces and line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Shield Gauntlets p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Super Commando (Elite)', ability: 'Shield Gauntlets', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Super Commando (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Professional p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Super Commando (Elite)', ability: 'Professional', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Super Commando (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Super Commando (Elite)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Super Commando (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Super Commando (Elite)', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Super Commando (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Super Commando (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Super Commando (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Super Commando (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Super Commando (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Super Commando (Elite)', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Super Commando (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Supercharge — condition traces', () => {
  it('Supercharge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Supercharge', ability: 'Supercharge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Supercharge', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Support Specialist — condition traces', () => {
  it('Support Specialist p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Support Specialist', ability: 'Support Specialist', part: '1', affects_self: 'TRUE', affects_others: 'a friendly DROID, TECHNICIAN, or TROOPER within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Support Specialist', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Survival Instincts — condition traces', () => {
  it('Survival Instincts p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Survival Instincts', ability: 'Survival Instincts', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Survival Instincts', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Take Cover — condition traces', () => {
  it('Take Cover p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Take Cover', ability: 'Take Cover', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Take Cover', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Take Cover p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Take Cover', ability: 'Take Cover', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Take Initiative — condition traces', () => {
  it('Take Initiative p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Take Initiative', ability: 'Take Initiative', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Take Initiative', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Take Position — condition traces', () => {
  it('Take Position p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Take Position', ability: 'Take Position', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Take it Down — condition traces', () => {
  it('Take it Down p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Take it Down', ability: 'Take it Down', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Take it Down', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Targeting Network — condition traces', () => {
  it('Targeting Network p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Targeting Network', ability: 'Targeting Network', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Targeting Network', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Targeting Network p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Targeting Network', ability: 'Targeting Network', part: '2', affects_self: 'TRUE', affects_others: 'your DROIDS', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Targeting Network', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Taron Malicos — condition traces', () => {
  it('Fallen Master p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Taron Malicos', ability: 'Fallen Master', part: '1', affects_self: 'TRUE', affects_others: 'friendly non-companion FORCE USER figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Taron Malicos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Madness p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Taron Malicos', ability: 'Madness', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Boulder Barrage p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Taron Malicos', ability: 'Boulder Barrage', part: '1', affects_self: 'TRUE', affects_others: 'any figure on the chosen space', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Surge p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Taron Malicos', ability: '+1 Surge', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Taron Malicos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Taron Malicos', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Taron Malicos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Taron Malicos', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Taron Malicos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Taron Malicos', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Taron Malicos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Tauntaun Rider — condition traces', () => {
  it('Mounted p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tauntaun Rider', ability: 'Mounted', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tauntaun Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Headbutt p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Tauntaun Rider', ability: 'Headbutt', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Useful Hide p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tauntaun Rider', ability: 'Useful Hide', part: '1', affects_self: 'TRUE', affects_others: 'up to friendly figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tauntaun Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tauntaun Rider', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tauntaun Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tauntaun Rider', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tauntaun Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy, +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tauntaun Rider', ability: 'Surge: +3 Accuracy, +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tauntaun Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tauntaun Rider', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tauntaun Rider', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Telekinetic Throw — condition traces', () => {
  it('Telekinetic Throw p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Telekinetic Throw', ability: 'Telekinetic Throw', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure in line of sight within 3 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Telekinetic Throw', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Terminal Network — condition traces', () => {
  it('Terminal Network p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Terminal Network', ability: 'Terminal Network', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Terminal Protocol — condition traces', () => {
  it('Terminal Protocol p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Terminal Protocol', ability: 'Terminal Protocol', part: '1', affects_self: 'FALSE', affects_others: 'each other figure and object in or adjacent to your space', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Terminal Protocol', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Terminal Protocol p2: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Terminal Protocol', ability: 'Terminal Protocol', part: '2', affects_self: 'FALSE', affects_others: 'each other figure and object in or adjacent to your space', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Terminal Protocol', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('The Armorer — condition traces', () => {
  it('Beskar Armor p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Armorer', ability: 'Beskar Armor', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Armorer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('This is the Way p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Armorer', ability: 'This is the Way', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 4 spaces of the Armorer', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Survival is Strength p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Armorer', ability: 'Survival is Strength', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure within 4 spaces of the Armorer', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Armorer', ability: 'Surge: +1 Damage, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Armorer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Armorer', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Armorer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Block token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Armorer', ability: 'Surge: gain 1 Block token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Armorer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Mobile p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Armorer', ability: 'Mobile', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Armorer', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('The Child — condition traces', () => {
  it('Force Heal p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Child', ability: 'Force Heal', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Force Exhaustion p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Child', ability: 'Force Exhaustion', part: '1', affects_self: 'TRUE', affects_others: 'the attacker, or a friendly figure with Clan of Two', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('The Grand Inquisitor — condition traces', () => {
  it('Precision p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Grand Inquisitor', ability: 'Precision', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent figure', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Precision p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Grand Inquisitor', ability: 'Precision', part: '2', affects_self: 'FALSE', affects_others: 'an adjacent figure', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Lightsaber Throw p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Grand Inquisitor', ability: 'Lightsaber Throw', part: '1', affects_self: 'FALSE', affects_others: 'a non-adjacent figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Deadly Spin p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Grand Inquisitor', ability: 'Deadly Spin', part: '1', affects_self: 'TRUE', affects_others: 'the defender', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Grand Inquisitor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Grand Inquisitor', ability: 'Surge: Pierce 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Grand Inquisitor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Grand Inquisitor', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Grand Inquisitor', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('The Mandalorian — condition traces', () => {
  it('Beskar Armor p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Mandalorian', ability: 'Beskar Armor', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Mandalorian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Disruptor Rifle p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'The Mandalorian', ability: 'Disruptor Rifle', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'The Mandalorian', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Disruptor Rifle p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'The Mandalorian', ability: 'Disruptor Rifle', part: '2', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Din\'s Wrist Flamethrower p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'The Mandalorian', ability: 'Din\'s Wrist Flamethrower', part: '1', affects_self: 'FALSE', affects_others: 'each other figure on or adjacent to the chosen space within 2', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'The Mandalorian', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Mandalorian', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Mandalorian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: gain 1 Block token p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Mandalorian', ability: 'Surge: gain 1 Block token', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Mandalorian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'The Mandalorian', ability: 'Surge: +2 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'The Mandalorian', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('There Is No Try — condition traces', () => {
  it('There Is No Try p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'There Is No Try', ability: 'There Is No Try', part: '1', affects_self: 'TRUE', affects_others: 'a friendly REBEL FORCE USER within 4 spaces', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('There is Another — condition traces', () => {
  it('There is Another p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'There is Another', ability: 'There is Another', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'There is Another', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('There is Another p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'There is Another', ability: 'There is Another', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Thrawn — condition traces', () => {
  it('Long-Laid Plans p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Thrawn', ability: 'Long-Laid Plans', part: '1', affects_self: 'TRUE', affects_others: 'friendly figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Thrawn', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Strategize p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Thrawn', ability: 'Strategize', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Thrawn', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Thrawn', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Thrawn', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Thrawn', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Thrawn', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge (2 surges): +3 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Thrawn', ability: 'Surge (2 surges): +3 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Thrawn', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('To the Limit — condition traces', () => {
  it('To the Limit p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'To the Limit', ability: 'To the Limit', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Tools for the Job — condition traces', () => {
  it('Tools for the Job p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tools for the Job', ability: 'Tools for the Job', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tools for the Job', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Tough Luck — condition traces', () => {
  it('Tough Luck p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Tough Luck', ability: 'Tough Luck', part: '1', affects_self: 'FALSE', affects_others: 'the opponent\'s figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Toxic Dart — condition traces', () => {
  it('Toxic Dart p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Toxic Dart', ability: 'Toxic Dart', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure within 3 spaces and in line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Trandoshan Hunter (Elite) — condition traces', () => {
  it('Relentless p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Trandoshan Hunter (Elite)', ability: 'Relentless', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('ACP Scattergun p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Trandoshan Hunter (Elite)', ability: 'ACP Scattergun', part: '1', affects_self: 'TRUE', affects_others: 'the target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Hardy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Trandoshan Hunter (Elite)', ability: 'Hardy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Trandoshan Hunter (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: you become Focused p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Trandoshan Hunter (Elite)', ability: 'Surge: you become Focused', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Trandoshan Hunter (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Trandoshan Hunter (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Trandoshan Hunter (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Trandoshan Hunter (Elite)', ability: 'Surge: +1 Damage, the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Trandoshan Hunter (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Trandoshan Hunter (Regular) — condition traces', () => {
  it('Relentless p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Trandoshan Hunter (Regular)', ability: 'Relentless', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Scattergun p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Trandoshan Hunter (Regular)', ability: 'Scattergun', part: '1', affects_self: 'TRUE', affects_others: 'the target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Trandoshan Hunter (Regular)', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Trandoshan Hunter (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Trandoshan Hunter (Regular)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Trandoshan Hunter (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Trandoshan Terror — condition traces', () => {
  it('Trandoshan Terror p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Trandoshan Terror', ability: 'Trandoshan Terror', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Transmit the Plans — condition traces', () => {
  it('Transmit the Plans p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Transmit the Plans', ability: 'Transmit the Plans', part: '1', affects_self: 'TRUE', affects_others: 'friendly figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Transmit the Plans', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Transmit the Plans p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Transmit the Plans', ability: 'Transmit the Plans', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Tress Hacnua — condition traces', () => {
  it('Krayt Dragon Fury p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Krayt Dragon Fury', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tress Hacnua', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Fyrnock Style p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Fyrnock Style', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tress Hacnua', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Fyrnock Style p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Fyrnock Style', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Tress Hacnua', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'tress_hacnua-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Leg Hydraulics p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Leg Hydraulics', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tress Hacnua', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Surge: +1 Damage, the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tress Hacnua', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave X p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Surge: Cleave X', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tress Hacnua', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover X p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Surge: Recover X', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tress Hacnua', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage, the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tress Hacnua', ability: 'Surge: +1 Damage, the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tress Hacnua', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Triangulate — condition traces', () => {
  it('Triangulate p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Triangulate', ability: 'Triangulate', part: '1', affects_self: 'TRUE', affects_others: 'up to 3 friendly DROIDS', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Triangulate', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Triangulate p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Triangulate', ability: 'Triangulate', part: '2', affects_self: 'FALSE', affects_others: 'a hostile figure within 5 spaces and line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Tusken Raider (Elite) — condition traces', () => {
  it('Tusken Cycler p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Tusken Raider (Elite)', ability: 'Tusken Cycler', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Tusken Raider (Elite)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tusken Raider (Elite)', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tusken Raider (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tusken Raider (Elite)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tusken Raider (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tusken Raider (Elite)', ability: 'Surge: the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tusken Raider (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Tusken Raider (Regular) — condition traces', () => {
  it('Tusken Cycler p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Tusken Raider (Regular)', ability: 'Tusken Cycler', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Tusken Raider (Regular)', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Surge: the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tusken Raider (Regular)', ability: 'Surge: the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tusken Raider (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Tusken Raider (Regular)', ability: 'Surge: Cleave 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Tusken Raider (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Ugnaught Tinkerer (Elite) — condition traces', () => {
  it('Spot Weld p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Elite)', ability: 'Spot Weld', part: '1', affects_self: 'TRUE', affects_others: 'the Junk Droid companion', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Overclock p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Elite)', ability: 'Overclock', part: '1', affects_self: 'TRUE', affects_others: 'the Junk Droid companion', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Scrap Battalion p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Elite)', ability: 'Scrap Battalion', part: '1', affects_self: 'TRUE', affects_others: 'the Junk Droid companion', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Elite)', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Elite)', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Ugnaught Tinkerer (Regular) — condition traces', () => {
  it('Spot Weld p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Regular)', ability: 'Spot Weld', part: '1', affects_self: 'TRUE', affects_others: 'the Junk Droid companion', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Scrap Battalion p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Regular)', ability: 'Scrap Battalion', part: '1', affects_self: 'TRUE', affects_others: 'the Junk Droid companion', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Regular)', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Ugnaught Tinkerer (Regular)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Ugnaught Tinkerer (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Unlimited Power — condition traces', () => {
  it('Unlimited Power p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Unlimited Power', ability: 'Unlimited Power', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure on the map', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Urgency — condition traces', () => {
  it('Urgency p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Urgency', ability: 'Urgency', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Urgency', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Utinni! — condition traces', () => {
  it('Utinni! p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Utinni!', ability: 'Utinni!', part: '1', affects_self: 'TRUE', affects_others: 'each friendly Jawa Scavenger', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Utinni!', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Utinni! p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Utinni!', ability: 'Utinni!', part: '2', affects_self: 'TRUE', affects_others: 'each friendly Jawa Scavenger', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Vanish — condition traces', () => {
  it('Vanish p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Vanish', ability: 'Vanish', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Vanish', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Vanish p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Vanish', ability: 'Vanish', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Vanish', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Verena Talos — condition traces', () => {
  it('Close Quarters p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Verena Talos', ability: 'Close Quarters', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure (attack type source) and the target', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Improvised Cover p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Verena Talos', ability: 'Improvised Cover', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Fighting Knife p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Verena Talos', ability: 'Fighting Knife', part: '1', affects_self: 'FALSE', affects_others: 'an adjacent hostile figure', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Evade p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Verena Talos', ability: '+1 Evade', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: 'Verena Talos', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'verena_talos-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Verena Talos', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Verena Talos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Verena Talos', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Verena Talos', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Veteran Instincts — condition traces', () => {
  it('Veteran Instincts p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Veteran Instincts', ability: 'Veteran Instincts', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Veteran Instincts', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Vinto Hreeda — condition traces', () => {
  it('Rapid Fire p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Vinto Hreeda', ability: 'Rapid Fire', part: '1', affects_self: 'FALSE', affects_others: 'the attack targets', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Vinto Hreeda', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Boltslinger p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Vinto Hreeda', ability: 'Boltslinger', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure other than the defender in LOS within 3 spaces', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage, the target becomes Weakened p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Vinto Hreeda', ability: 'Surge: +1 Damage, the target becomes Weakened', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Vinto Hreeda', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2, the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Vinto Hreeda', ability: 'Surge: Pierce 2, the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Vinto Hreeda', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, you become Hidden p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Vinto Hreeda', ability: 'Surge: +2 Accuracy, you become Hidden', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Vinto Hreeda', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wampa (Elite) — condition traces', () => {
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Elite)', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Hunger p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wampa (Elite)', ability: 'Hunger', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Non-Sentient p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Elite)', ability: 'Non-Sentient', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Reach p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Elite)', ability: 'Reach', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Elite)', ability: '+2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 3 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Elite)', ability: 'Surge: Cleave 3', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Weakened, the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Elite)', ability: 'Surge: the target becomes Weakened, the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wampa (Regular) — condition traces', () => {
  it('Efficient Travel p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Regular)', ability: 'Efficient Travel', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Hunger p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wampa (Regular)', ability: 'Hunger', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Non-Sentient p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Regular)', ability: 'Non-Sentient', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Regular)', ability: '+1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Stunned p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Regular)', ability: 'Surge: the target becomes Stunned', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wampa (Regular)', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wampa (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Weequay Pirate (Elite) — condition traces', () => {
  it('Prowl p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Elite)', ability: 'Prowl', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Raider p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Elite)', ability: 'Raider', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Elite)', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Elite)', ability: 'Surge: Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Elite)', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Weequay Pirate (Regular) — condition traces', () => {
  it('Raider p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Regular)', ability: 'Raider', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Regular)', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Weequay Pirate (Regular)', ability: 'Surge: +1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Weequay Pirate (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Whistling Birds — condition traces', () => {
  it('Whistling Birds p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Whistling Birds', ability: 'Whistling Birds', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Whistling Birds', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Whistling Birds p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Whistling Birds', ability: 'Whistling Birds', part: '2', affects_self: 'TRUE', affects_others: 'up to 3 figures within 2 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Whistling Birds', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wild Attack — condition traces', () => {
  it('Wild Attack p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wild Attack', ability: 'Wild Attack', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wild Attack', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wild Fire — condition traces', () => {
  it('Wild Fire p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wild Fire', ability: 'Wild Fire', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wild Fire', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wild Fury — condition traces', () => {
  it('Wild Fury p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wild Fury', ability: 'Wild Fury', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wild Fury', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Wild Fury p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wild Fury', ability: 'Wild Fury', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wild Fury', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Windfall — condition traces', () => {
  it('Windfall p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Windfall', ability: 'Windfall', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Windfall p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Windfall', ability: 'Windfall', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Wing Guard (Elite) — condition traces', () => {
  it('Keep the Peace p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wing Guard (Elite)', ability: 'Keep the Peace', part: '1', affects_self: 'FALSE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Bespin Security p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wing Guard (Elite)', ability: 'Bespin Security', part: '1', affects_self: 'TRUE', affects_others: 'an adjacent friendly LEADER or SCUM TROOPER', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wing Guard (Elite)', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wing Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wing Guard (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wing Guard (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wing Guard (Regular) — condition traces', () => {
  it('Keep the Peace p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wing Guard (Regular)', ability: 'Keep the Peace', part: '1', affects_self: 'TRUE', affects_others: 'the attacker', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wing Guard (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wing Guard (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Recover 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wing Guard (Regular)', ability: 'Surge: Recover 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wing Guard (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wookiee Rage — condition traces', () => {
  it('Wookiee Rage p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Wookiee Rage', ability: 'Wookiee Rage', part: '1', affects_self: 'FALSE', affects_others: 'up to 3 hostile figures adjacent to you', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Wookiee Rage', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('Wookiee Warrior (Elite) — condition traces', () => {
  it('Fury p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wookiee Warrior (Elite)', ability: 'Fury', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wookiee Warrior (Elite)', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wookiee Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wookiee Warrior (Elite)', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wookiee Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wookiee Warrior (Elite)', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wookiee Warrior (Elite)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Wookiee Warrior (Regular) — condition traces', () => {
  it('Fury p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wookiee Warrior (Regular)', ability: 'Fury', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Surge: +1 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wookiee Warrior (Regular)', ability: 'Surge: +1 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wookiee Warrior (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: the target becomes Bleeding p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wookiee Warrior (Regular)', ability: 'Surge: the target becomes Bleeding', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wookiee Warrior (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Cleave 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Wookiee Warrior (Regular)', ability: 'Surge: Cleave 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Wookiee Warrior (Regular)', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Worth Every Credit — condition traces', () => {
  it('Worth Every Credit p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Worth Every Credit', ability: 'Worth Every Credit', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Worth Every Credit p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Worth Every Credit', ability: 'Worth Every Credit', part: '2', affects_self: 'TRUE', affects_others: 'the next hostile figure defeated during this activation', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Wreak Vengeance — condition traces', () => {
  it('Wreak Vengeance p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Wreak Vengeance', ability: 'Wreak Vengeance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Yoda — condition traces', () => {
  it('Calming Presence p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Yoda', ability: 'Calming Presence', part: '1', affects_self: 'TRUE', affects_others: 'a friendly REBEL FORCE USER figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Yoda', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Wisdom p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Yoda', ability: 'Wisdom', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Yoda', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Do or Do Not p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Yoda', ability: 'Do or Do Not', part: '1', affects_self: 'FALSE', affects_others: 'another friendly REBEL FORCE USER within 4 spaces', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Yoda', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Force Deflection p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Yoda', ability: 'Force Deflection', part: '1', affects_self: 'FALSE', affects_others: 'the attacking figure', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('You Will Not Deny Me — condition traces', () => {
  it('You Will Not Deny Me p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'You Will Not Deny Me', ability: 'You Will Not Deny Me', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('You Will Not Deny Me p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'You Will Not Deny Me', ability: 'You Will Not Deny Me', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('You Will Not Deny Me p4: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'You Will Not Deny Me', ability: 'You Will Not Deny Me', part: '4', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('You Will Not Deny Me p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'You Will Not Deny Me', ability: 'You Will Not Deny Me', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('Zeb Orrelios — condition traces', () => {
  it('Bo-Rifle Staff Strike p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: 'Zeb Orrelios', ability: 'Bo-Rifle Staff Strike', part: '1', affects_self: 'FALSE', affects_others: 'the attack target', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: 'Zeb Orrelios', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Lasat-Honor Guard p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Zeb Orrelios', ability: 'Lasat-Honor Guard', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zeb Orrelios', ability: '+3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zeb Orrelios', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zeb Orrelios', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zeb Orrelios', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Recover 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zeb Orrelios', ability: 'Surge: +2 Accuracy, Recover 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zeb Orrelios', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('Zuckuss — condition traces', () => {
  it('Mystic Hunter p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zuckuss', ability: 'Mystic Hunter', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zuckuss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Shared Calculations p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Zuckuss', ability: 'Shared Calculations', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Stun Net p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: 'Zuckuss', ability: 'Stun Net', part: '1', affects_self: 'FALSE', affects_others: 'the target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zuckuss', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zuckuss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +3 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zuckuss', ability: 'Surge: +3 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zuckuss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zuckuss', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zuckuss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Pierce 2 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: 'Zuckuss', ability: 'Surge: Pierce 2', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: 'Zuckuss', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[A New Hope] — condition traces', () => {
  it('[A New Hope] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[A New Hope]', ability: '[A New Hope]', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Advanced Com Systems] — condition traces', () => {
  it('[Advanced Com Systems] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Advanced Com Systems]', ability: '[Advanced Com Systems]', part: '1', affects_self: 'TRUE', affects_others: 'other friendly figures within 3', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Balance of the Force] — condition traces', () => {
  it('[Balance of the Force] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Balance of the Force]', ability: '[Balance of the Force]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Balance of the Force]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Beast Tamer] — condition traces', () => {
  it('[Beast Tamer] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Beast Tamer]', ability: '[Beast Tamer]', part: '1', affects_self: 'TRUE', affects_others: 'a CREATURE figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Beast Tamer] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Beast Tamer]', ability: '[Beast Tamer]', part: '2', affects_self: 'TRUE', affects_others: 'a NON-SENTIENT figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Black Market] — condition traces', () => {
  it('[Black Market] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Black Market]', ability: '[Black Market]', part: '1', affects_self: 'TRUE', affects_others: 'a friendly SMUGGLER', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Channel the Force] — condition traces', () => {
  it('[Channel the Force] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Channel the Force]', ability: '[Channel the Force]', part: '1', affects_self: 'TRUE', affects_others: 'a friendly FORCE USER', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Clan of Two] — condition traces', () => {
  it('[Clan of Two] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Clan of Two]', ability: '[Clan of Two]', part: '1', affects_self: 'TRUE', affects_others: 'The Child companion', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Clan of Two] p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Clan of Two]', ability: '[Clan of Two]', part: '2', affects_self: 'TRUE', affects_others: 'The Child figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Clan of Two]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('[Clan of Two] p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Clan of Two]', ability: '[Clan of Two]', part: '3', affects_self: 'TRUE', affects_others: 'The Child', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Clan of Two] p4: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Clan of Two]', ability: '[Clan of Two]', part: '4', affects_self: 'TRUE', affects_others: 'a UNIQUE figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Combat Suit] — condition traces', () => {
  it('[Combat Suit] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Combat Suit]', ability: '[Combat Suit]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Cross Training] — condition traces', () => {
  it('[Cross Training] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Cross Training]', ability: '[Cross Training]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Cross Training]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('[Cross Training] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Cross Training]', ability: '[Cross Training]', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Devious Scheme] — condition traces', () => {
  it('[Devious Scheme] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Devious Scheme]', ability: '[Devious Scheme]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Doubt] — condition traces', () => {
  it('[Doubt] p1: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: '[Doubt]', ability: '[Doubt]', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: '[Doubt]', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('[Doubt] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Doubt]', ability: '[Doubt]', part: '2', affects_self: 'FALSE', affects_others: 'a hostile figure', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Driven by Hatred] — condition traces', () => {
  it('[Driven by Hatred] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Driven by Hatred]', ability: '[Driven by Hatred]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Driven by Hatred]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('[Driven by Hatred] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Driven by Hatred]', ability: '[Driven by Hatred]', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Driven by Hatred] p3: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Driven by Hatred]', ability: '[Driven by Hatred]', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Driven by Hatred]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Explosive Armaments] — condition traces', () => {
  it('[Explosive Armaments] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Explosive Armaments]', ability: '[Explosive Armaments]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Extra Armor] — condition traces', () => {
  it('[Extra Armor] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Extra Armor]', ability: '[Extra Armor]', part: '1', affects_self: 'TRUE', affects_others: 'friendly figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Extra Armor]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Feeding Frenzy] — condition traces', () => {
  it('[Feeding Frenzy] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Feeding Frenzy]', ability: '[Feeding Frenzy]', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Feeding Frenzy] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Feeding Frenzy]', ability: '[Feeding Frenzy]', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[First Strike] — condition traces', () => {
  it('[First Strike] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[First Strike]', ability: '[First Strike]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[First Strike]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Flame Trooper] — condition traces', () => {
  it('Incinerate p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: 'Incinerate', part: '1', affects_self: 'TRUE', affects_others: 'each figure that suffered damage since this attack was declared', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Fireproof p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: 'Fireproof', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Priority Target p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: 'Priority Target', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Bleed p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: 'Bleed', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('+2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: '+2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: 'Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Flame Trooper]', ability: 'Surge: Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Flame Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Focused on the Kill] — condition traces', () => {
  it('[Focused on the Kill] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Focused on the Kill]', ability: '[Focused on the Kill]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Focused on the Kill]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('[Focused on the Kill] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Focused on the Kill]', ability: '[Focused on the Kill]', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Fury of Kashyyyk] — condition traces', () => {
  it('[Fury of Kashyyyk] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Fury of Kashyyyk]', ability: '[Fury of Kashyyyk]', part: '1', affects_self: 'TRUE', affects_others: 'friendly WOOKIEES', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Fury of Kashyyyk]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('[Fury of Kashyyyk] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Fury of Kashyyyk]', ability: '[Fury of Kashyyyk]', part: '2', affects_self: 'TRUE', affects_others: 'a friendly WOOKIEE', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Fury of Kashyyyk] p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Fury of Kashyyyk]', ability: '[Fury of Kashyyyk]', part: '3', affects_self: 'TRUE', affects_others: 'a friendly elite WOOKIEE; the defender within 2', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Headhunter] — condition traces', () => {
  it('[Headhunter] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Headhunter]', ability: '[Headhunter]', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Heavy Fire] — condition traces', () => {
  it('[Heavy Fire] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Heavy Fire]', ability: '[Heavy Fire]', part: '1', affects_self: 'FALSE', affects_others: 'hostile figures within 2 of the target space', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Heir to the Jedi] — condition traces', () => {
  it('[Heir to the Jedi] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Heir to the Jedi]', ability: '[Heir to the Jedi]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Heir to the Jedi] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Heir to the Jedi]', ability: '[Heir to the Jedi]', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Heir to the Jedi] p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Heir to the Jedi]', ability: '[Heir to the Jedi]', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Heroic Effort] — condition traces', () => {
  it('[Heroic Effort] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Heroic Effort]', ability: '[Heroic Effort]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Imperial Citadel] — condition traces', () => {
  it('[Imperial Citadel] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Imperial Citadel]', ability: '[Imperial Citadel]', part: '1', affects_self: 'TRUE', affects_others: 'a friendly Imperial figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Imperial Citadel] p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Imperial Citadel]', ability: '[Imperial Citadel]', part: '2', affects_self: 'TRUE', affects_others: 'a friendly Imperial figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Imperial Citadel]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('[Imperial Citadel] p3: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Imperial Citadel]', ability: '[Imperial Citadel]', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Imperial Citadel]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Imperial Retrofitting] — condition traces', () => {
  it('[Imperial Retrofitting] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Imperial Retrofitting]', ability: '[Imperial Retrofitting]', part: '1', affects_self: 'TRUE', affects_others: 'a friendly MASSIVE VEHICLE', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('[Imperial Retrofitting] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Imperial Retrofitting]', ability: '[Imperial Retrofitting]', part: '2', affects_self: 'TRUE', affects_others: 'a friendly MASSIVE VEHICLE', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Indentured Jester] — condition traces', () => {
  it('[Indentured Jester] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Indentured Jester]', ability: '[Indentured Jester]', part: '1', affects_self: 'TRUE', affects_others: 'Salacious B. Crumb companion', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Last Resort] — condition traces', () => {
  it('[Last Resort] p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Last Resort]', ability: '[Last Resort]', part: '1', affects_self: 'FALSE', affects_others: 'each figure and object on or adjacent to that figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Lie in Ambush] — condition traces', () => {
  it('[Lie in Ambush] p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Lie in Ambush]', ability: '[Lie in Ambush]', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Lie in Ambush]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('[Lie in Ambush] p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Lie in Ambush]', ability: '[Lie in Ambush]', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Mortar Trooper] — condition traces', () => {
  it('Fire Mission p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Mortar Trooper]', ability: 'Fire Mission', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Mortar Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Guidance Systems p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Mortar Trooper]', ability: 'Guidance Systems', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Haul p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Mortar Trooper]', ability: 'Haul', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('+1 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Mortar Trooper]', ability: '+1 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Mortar Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Mortar Trooper]', ability: 'Surge: +2 Accuracy', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Mortar Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Mortar Trooper]', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Mortar Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Motivation] — condition traces', () => {
  it('Motivation p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Motivation]', ability: 'Motivation', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure with lower figure cost and line of sight', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Nemik\'s Manifesto] — condition traces', () => {
  it('Nemik\'s Manifesto p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Nemik\'s Manifesto]', ability: 'Nemik\'s Manifesto', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Nemik\'s Manifesto]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Nemik\'s Manifesto p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Nemik\'s Manifesto]', ability: 'Nemik\'s Manifesto', part: '2', affects_self: 'TRUE', affects_others: 'a friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Nemik\'s Manifesto]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[On a Diplomatic Mission] — condition traces', () => {
  it('On a Diplomatic Mission p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[On a Diplomatic Mission]', ability: 'On a Diplomatic Mission', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('On a Diplomatic Mission p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[On a Diplomatic Mission]', ability: 'On a Diplomatic Mission', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('On a Diplomatic Mission p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[On a Diplomatic Mission]', ability: 'On a Diplomatic Mission', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('On a Diplomatic Mission p4: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[On a Diplomatic Mission]', ability: 'On a Diplomatic Mission', part: '4', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Orbital Bombardment] — condition traces', () => {
  it('Orbital Bombardment p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Orbital Bombardment]', ability: 'Orbital Bombardment', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Orbital Bombardment]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Orbital Bombardment p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Orbital Bombardment]', ability: 'Orbital Bombardment', part: '2', affects_self: 'TRUE', affects_others: 'each figure on a chosen space', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Orbital Bombardment]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Overwatch] — condition traces', () => {
  it('Overwatch p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Overwatch]', ability: 'Overwatch', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Overwatch]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Overwatch p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Overwatch]', ability: 'Overwatch', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Overwatch p3: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Overwatch]', ability: 'Overwatch', part: '3', affects_self: 'FALSE', affects_others: 'a hostile figure entering on or adjacent to the Overwatch token', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Prey on the Weak] — condition traces', () => {
  it('Prey on the Weak p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Prey on the Weak]', ability: 'Prey on the Weak', part: '1', affects_self: 'TRUE', affects_others: 'the attack target', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Punishing Strike] — condition traces', () => {
  it('Punishing Strike p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Punishing Strike]', ability: 'Punishing Strike', part: '1', affects_self: 'TRUE', affects_others: 'a figure with the condition', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Rebel High Command] — condition traces', () => {
  it('Rebel High Command p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Rebel High Command]', ability: 'Rebel High Command', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Rebel High Command]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Rogue One] — condition traces', () => {
  it('Rogue One p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Rogue One]', ability: 'Rogue One', part: '1', affects_self: 'TRUE', affects_others: 'a listed friendly figure attacking', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Rogue One p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Rogue One]', ability: 'Rogue One', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Rogue Smuggler] — condition traces', () => {
  it('Rogue Smuggler p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Rogue Smuggler]', ability: 'Rogue Smuggler', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Rogue Smuggler]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Rogue Smuggler p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Rogue Smuggler]', ability: 'Rogue Smuggler', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Rogue Smuggler]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Rule by Fear] — condition traces', () => {
  it('Rule by Fear p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Rule by Fear]', ability: 'Rule by Fear', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Scavenged Walker] — condition traces', () => {
  it('Scavenged Walker p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Scavenged Walker]', ability: 'Scavenged Walker', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Scavenged Walker]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Scavenged Walker p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Scavenged Walker]', ability: 'Scavenged Walker', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Scavenged Walker]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Scavenged Weaponry] — condition traces', () => {
  it('Scavenged Weaponry p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Scavenged Weaponry]', ability: 'Scavenged Weaponry', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Scavenged Weaponry]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Scavenged Weaponry p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Scavenged Weaponry]', ability: 'Scavenged Weaponry', part: '2', affects_self: 'TRUE', affects_others: 'a friendly DROID or VEHICLE group', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Smuggler\'s Run] — condition traces', () => {
  it('Smuggler\'s Run p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Smuggler\'s Run]', ability: 'Smuggler\'s Run', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Smuggling Compartment] — condition traces', () => {
  it('Smuggling Compartment p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Smuggling Compartment]', ability: 'Smuggling Compartment', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Smuggling Compartment p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Smuggling Compartment]', ability: 'Smuggling Compartment', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Spectre Cell] — condition traces', () => {
  it('Spectre Cell p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Spectre Cell]', ability: 'Spectre Cell', part: '1', affects_self: 'TRUE', affects_others: 'friendly figures', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Spectre Cell]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Spectre Cell p2: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: '[Spectre Cell]', ability: 'Spectre Cell', part: '2', affects_self: 'FALSE', affects_others: 'another friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: '[Spectre Cell]', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('[Suppressive Fire] — condition traces', () => {
  it('Suppressive Fire p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Suppressive Fire]', ability: 'Suppressive Fire', part: '1', affects_self: 'FALSE', affects_others: 'the attack target, and another SMALL friendly figure within 3', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Survivalist] — condition traces', () => {
  it('Survivalist p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Survivalist]', ability: 'Survivalist', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Survivalist]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Survivalist p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Survivalist]', ability: 'Survivalist', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Targeting Computer] — condition traces', () => {
  it('Targeting Computer p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Targeting Computer]', ability: 'Targeting Computer', part: '1', affects_self: 'TRUE', affects_others: 'figures in this group', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Targeting Computer]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Technician Training] — condition traces', () => {
  it('Technician Training p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Technician Training]', ability: 'Technician Training', part: '1', affects_self: 'TRUE', affects_others: 'listed named figures in all armies', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Technician Training]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Temporary Alliance (M)] — condition traces', () => {
  it('Temporary Alliance (M) p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Temporary Alliance (M)]', ability: 'Temporary Alliance (M)', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Temporary Alliance (M)]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Temporary Alliance] — condition traces', () => {
  it('Temporary Alliance p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Temporary Alliance]', ability: 'Temporary Alliance', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Temporary Alliance]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[The Darksaber] — condition traces', () => {
  it('The Darksaber p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[The Darksaber]', ability: 'The Darksaber', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('The Darksaber p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[The Darksaber]', ability: 'The Darksaber', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[The Darksaber]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('The Darksaber p3: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[The Darksaber]', ability: 'The Darksaber', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[The Darksaber]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[The General\'s Ranks] — condition traces', () => {
  it('The General\'s Ranks p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[The General\'s Ranks]', ability: 'The General\'s Ranks', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('The General\'s Ranks p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[The General\'s Ranks]', ability: 'The General\'s Ranks', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Trusted Ally] — condition traces', () => {
  it('Trusted Ally p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Trusted Ally]', ability: 'Trusted Ally', part: '1', affects_self: 'FALSE', affects_others: 'another friendly figure within 3', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Trusted Ally p2: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: '[Trusted Ally]', ability: 'Trusted Ally', part: '2', affects_self: 'FALSE', affects_others: 'an adjacent friendly figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: '[Trusted Ally]', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
});

describe('[Under Duress] — condition traces', () => {
  it('Under Duress p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Under Duress]', ability: 'Under Duress', part: '1', affects_self: 'FALSE', affects_others: 'a hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Under Duress p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Under Duress]', ability: 'Under Duress', part: '2', affects_self: 'FALSE', affects_others: 'a hostile figure', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Unshakable] — condition traces', () => {
  it('Unshakable p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Unshakable]', ability: 'Unshakable', part: '1', affects_self: 'TRUE', affects_others: 'one of your figures with figure cost 9 or greater', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Vader\'s Finest] — condition traces', () => {
  it('Vader\'s Finest p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Vader\'s Finest]', ability: 'Vader\'s Finest', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Vader\'s Finest]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Vader\'s Finest p2: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Vader\'s Finest]', ability: 'Vader\'s Finest', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});

describe('[Wookiee Avenger] — condition traces', () => {
  it('Wookiee Avenger p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Wookiee Avenger]', ability: 'Wookiee Avenger', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Wookiee Avenger]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Wookiee Avenger p2: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Wookiee Avenger]', ability: 'Wookiee Avenger', part: '2', affects_self: 'TRUE', affects_others: 'None', attack_side: 'defender', conditional: '', limit: '' });
    assert.ok(cond(G, { defenderDcName: '[Wookiee Avenger]', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'wookiee_avenger-1-0' } }), 'owner active → fires');
    assert.ok(!cond(G, { defenderDcName: 'Random Figure', attackerPlayerNum: 2, defenderPlayerNum: 1, target: { figureKey: 'Random-1-0' } }), 'non-owner → blocked');
  });
  it('Wookiee Avenger p3: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Wookiee Avenger]', ability: 'Wookiee Avenger', part: '3', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Wookiee Avenger]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Wookiee Avenger p4: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Wookiee Avenger]', ability: 'Wookiee Avenger', part: '4', affects_self: 'TRUE', affects_others: 'None', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Wookiee Avenger]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Z-6 Trooper] — condition traces', () => {
  it('Z-6 Trooper p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Z-6 Trooper]', ability: 'Z-6 Trooper', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Z-6 Trooper p2: affects_self=FALSE → owner self-pred blocked', () => {
    const cond = conditionForRow({ card: '[Z-6 Trooper]', ability: 'Z-6 Trooper', part: '2', affects_self: 'FALSE', affects_others: 'the defending figure', attack_side: 'None', conditional: '', limit: '' });
    assert.ok(!cond(G, { attackerDcName: '[Z-6 Trooper]', attackerPlayerNum: 1 }), 'owner self-pred must not fire');
  });
  it('Blast 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Z-6 Trooper]', ability: 'Blast 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Z-6 Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Damage p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Z-6 Trooper]', ability: 'Surge: +2 Damage', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Z-6 Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
  it('Surge: +2 Accuracy, Pierce 1 p1: affects_self=TRUE → fires when owner is active', () => {
    const cond = conditionForRow({ card: '[Z-6 Trooper]', ability: 'Surge: +2 Accuracy, Pierce 1', part: '1', affects_self: 'TRUE', affects_others: 'None', attack_side: 'attacker', conditional: '', limit: '' });
    assert.ok(cond(G, { attackerDcName: '[Z-6 Trooper]', attackerPlayerNum: 1 }), 'owner active → fires');
    assert.ok(!cond(G, { attackerDcName: 'Random Figure', attackerPlayerNum: 1 }), 'non-owner → blocked');
  });
});

describe('[Zillo Technique] — condition traces', () => {
  it('Pierce Reduction p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Zillo Technique]', ability: 'Pierce Reduction', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure that is defending', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
  it('Block Boost p1: conditional guard — smoke only (no crash)', () => {
    const row = { card: '[Zillo Technique]', ability: 'Block Boost', part: '1', affects_self: 'TRUE', affects_others: 'a friendly figure that is defending', attack_side: 'defender', conditional: '', limit: '' };
    assert.doesNotThrow(() => conditionForRow(row)(G, {}));
  });
});
