DISCORD_TOKEN=your_bot_token_here

# Optional: PostgreSQL for game persistence (required on Railway, omit for local file-based storage)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Bot-logs: the bot @mentions the "Bothelpers" role by name when a game error is logged. Create a role named "Bothelpers" (or "bothelpers") in your server so the team gets notified.
